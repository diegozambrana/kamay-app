import type { Role } from "@/types";

/** Los roles de una membresía dichos como en *Usuarios y roles*. */
export const ROLE_LABELS: Record<Role, string> = {
  owner: "Dueña o dueño",
  assistant: "Ayudante",
};
