> Cada tarea de prueba nombra los escenarios del delta spec que cubre (convención nº 12: ningún escenario sin prueba referenciada). Los escenarios viven en `specs/fair-mode/spec.md` y `specs/orders/spec.md` de este cambio.

## 0. Requisitos previos

- [x] 0.1 Verificar que **KAM-10** está en la historia de la rama de trabajo: existen la tabla `payments` y la columna `paid` en `order_totals`. Sin eso, `create_direct_sale` no puede insertar el cobro (`design.md` — Migration Plan, paso 1). Si no está, detener aquí. — *Cumplido en `27e47aa`; `20260903180000_payments.sql`. El criterio original decía «fusionado en `main`», que no aplica a este repo: `main` es solo el commit inicial y las ramas KAM se encadenan.*
- [x] 0.2 Verificar que **KAM-11** está en la historia de la rama de trabajo y contrastar su API real contra el diseño. — *Cumplido en `925892f`. El contrato supuesto difería en cinco puntos; `design.md` decisión 5 está reescrito contra la API real, y las divergencias que obligaban a diseñar algo se resolvieron en las decisiones 6 (`deadlineMs: 0`), 12 (snapshot de feria) y 13 (indicador propio filtrado).*
- [x] 0.3 Confirmar que `create_order` sigue fijando `'order'` literal y no acepta `kind` como parámetro (`20260903120000_order_entry.sql`). Es la premisa de la decisión 2; si cambió, el diseño se revisa. — *Cumplido: KAM-11 la redefinió en `20260903210000_offline_sync.sql` solo para añadir idempotencia, conservando `'order'` literal. Ese bloque de idempotencia es el patrón que `create_direct_sale` debe replicar en la tarea 1.8.*

## 1. Migración de base de datos

- [x] 1.1 Crear `supabase/migrations/<AAAAMMDDHHMMSS>_direct_sales.sql` (archivo nuevo; no editar ninguna migración existente). **No crea ninguna tabla ni columna**: `orders`, `order_items` y `payments` ya sirven tal cual (design, decisión 1). Encabezar el archivo con la referencia al esquema canónico § 9 y a las decisiones que aplica.
- [x] 1.2 Crear la vista `best_selling_products` con `security_invoker = true` (convención nº 4): cantidad vendida por `item_id`, `variant_id`, `organization_id` y `business_line_id`, sumando `order_items` de pedidos y ventas directas no archivados con `occurred_at` en los últimos 90 días. La ventana de 90 días se declara **una sola vez** aquí (design, decisión 4).
- [x] 1.3 Escribir en la misma migración `create or replace function create_direct_sale(p_sale jsonb, p_items jsonb, p_payment jsonb) returns uuid language plpgsql security invoker`, siguiendo la estructura de `create_order`: comprobación explícita de `is_member(v_org)` (convención nº 2), rechazo si faltan organización o línea, y rechazo si la lista de líneas viene vacía o no es un arreglo.
- [x] 1.4 En esa función, resolver el estado con `select s.id from resolve_statuses(v_org, v_line, 'order') s where s.kind = 'final' order by s.position limit 1`, y fallar con mensaje comprensible si no hay ninguno (design, decisión 3). **Nunca comparar por nombre** (convención nº 5).
- [x] 1.5 Insertar la fila en `orders` con `kind = 'direct_sale'` literal, `contact_id` opcional, `sales_channel_id` desde el argumento, `delivery_mode` y `due_date` nulos, y `occurred_at` tomado del cliente con `now()` como respaldo (convención nº 9). El `id` se acepta del cliente si viene.
- [x] 1.6 Insertar las líneas en `order_items` con el `unit_price` tal como se registró, aceptando `id` de cliente en cada una.
- [x] 1.7 Insertar el cobro en `payments` con `direction = 'in'`, `order_id` de la venta, `amount`, `method` y `occurred_at` del argumento, **solo si `p_payment` no es nulo** (design, decisión 2). Todo dentro de la misma función, y por tanto de la misma transacción.
- [x] 1.8 Añadir `on conflict (id) do nothing` en la inserción de la venta como red de seguridad de la idempotencia por `uuid`, y devolver el `id` de la venta en todos los casos (design, decisión 5).
- [x] 1.9 Escribir `grant execute` de `create_direct_sale` a `authenticated` y `grant select` de `best_selling_products` a `authenticated, service_role`, siguiendo el patrón de `20260903120000_order_entry.sql`. **No se crea ninguna política nueva**: las de `orders`, `order_items` y `payments` ya gobiernan estas filas.
- [x] 1.10 Ampliar `supabase/seed.sql` con ventas directas de Alfarería —algunas cobradas en el acto, una con cobro parcial, una sin cobro— y con historial suficiente para que el orden de la cuadrícula sea observable.
- [x] 1.11 Ejecutar `graphify update .` y versionar el resultado (convención nº 6).

## 2. Pruebas pgTAP de la migración

- [x] 2.1 `supabase/tests/direct_sale_entry.test.sql`, con su propia organización (regla de pruebas del proyecto): alta de la venta. Cubre *La venta directa es un pedido de tipo `direct_sale` nacido en estado final* → «Nace en el estado final de su línea», «Renombrar el estado final no cambia el comportamiento», «Venta sin cliente», «Venta con cliente», «La línea sigue siendo obligatoria», «Numerada como cualquier pedido».
- [x] 2.2 Ampliar `direct_sale_entry.test.sql` con la atomicidad y el cobro, usando `throws_ok` de cuatro argumentos. Cubre *La venta y su cobro se registran en una sola operación* → «Venta cobrada en el acto», «Un fallo deja todo como estaba», «Venta sin líneas», «Cobro parcial», «Venta sin cobro», «La hora real es la del hecho», «Registro en la bitácora».
- [x] 2.3 Ampliar `direct_sale_entry.test.sql` con la idempotencia: invocar dos veces con el mismo `id` de venta y comprobar que existe una sola fila y un solo cobro. Cubre *Vender sin conexión no falla ni duplica* → «Reintento por fallo de red» en su parte de base de datos.
- [x] 2.4 `supabase/tests/direct_sale_access.test.sql`: permisos y aislamiento. Cubre *Aislamiento y roles en el modo feria* → «El ayudante vende», «Miembro de otra organización», «Intento de borrado», «La cuadrícula no cruza organizaciones».
- [x] 2.5 `supabase/tests/best_selling.test.sql`: la vista de más vendidos. Cubre *Cuadrícula de productos vendibles ordenada por más vendidos* → «Orden por ventas recientes» y, en su parte de datos, «Producto de otra línea»; comprobar además que `security_invoker = true` está declarado.
- [x] 2.6 `supabase/tests/order_entry_kinds.test.sql`: la separación de las dos vías de alta. Cubre el delta de `orders` → *El alta es una sola operación y el estado inicial lo asigna la base* → «El alta de pedidos no crea ventas directas», «El alta de pedidos exige cliente». Verificar además que los escenarios de idempotencia que KAM-11 añadió a ese requisito —«El mismo pedido enviado dos veces», «El reenvío no ensucia la bitácora», «Identificador que pertenece a otra organización»— **siguen pasando**: el bloque `MODIFIED` los arrastra sin cambiarlos.
- [x] 2.7 Ampliar `supabase/tests/order_totals.test.sql` (o crear `direct_sale_revenue.test.sql` si aquel no existe) con los ingresos mixtos. Cubre el delta de `orders` → *Las consultas de ingresos incluyen las ventas directas* → «Ingresos de la línea», «Una sola fuente», «El cobrado también suma», «La venta directa archivada no suma».

## 3. Reglas puras y pruebas unitarias

- [x] 3.1 Crear `lib/fair/cart.ts` con las operaciones puras del carrito: `addLine`, `removeLine`, `clear`, `cartTotal` y `cartUnits`, agrupando por `(item_id, variant_id)` e incrementando cantidad en lugar de duplicar líneas.
- [x] 3.2 `lib/fair/cart.test.ts`. Cubre *Carrito y cobro en cuatro interacciones o menos* → «Tocar dos veces el mismo producto», «El total sigue al carrito», «Quitar una línea», «Monto propuesto» en su parte de cálculo. Incluir casos con decimales que delaten un redondeo de coma flotante.
- [x] 3.3 Crear `lib/fair/grid-order.ts` con la función pura que ordena la cuadrícula: primero por cantidad vendida descendente, después por nombre para los productos sin ventas. Cubre la parte de orden de la decisión 4.
- [x] 3.4 `lib/fair/grid-order.test.ts`. Cubre *Cuadrícula de productos vendibles ordenada por más vendidos* → «Orden por ventas recientes», «Producto sin ventas».
- [x] 3.5 Crear `lib/fair/sale-envelope.ts`: construye el sobre encolable de una venta —`id` `uuid` generado en el cliente, `occurredAt` fijado por el cliente, líneas y cobro dentro— y su `sale-schema.ts` de Zod (líneas no vacías, cantidades positivas, precios no negativos, monto de cobro no negativo).
- [x] 3.6 `lib/fair/sale-envelope.test.ts`. Cubre *La venta y su cobro se registran en una sola operación* → «La hora real es la del hecho», «Venta sin líneas», «Venta sin cobro» en su parte de forma del sobre; y el supuesto de que **el `id` del sobre es el `uuid` de la venta** (design, decisión 5).

## 4. Servicio y acciones de servidor

- [x] 4.1 Crear `services/fair/fair-sale-service.ts` con el `SupabaseClient` inyectado (ninguna consulta a Supabase fuera de `services/`): `listSellableProducts(orgId, lineId)` —productos no archivados de la línea o compartidos, con precio de venta, en `left join` con `best_selling_products` (design, decisión 4)— y `createDirectSale(envelope)` invocando `create_direct_sale`.
- [x] 4.2 `services/fair/fair-sale-service.test.ts` con cliente simulado: forma de la consulta, filtro explícito por `organization_id` aunque RLS ya filtre, exclusión de insumos, activos, archivados y productos sin precio. Cobertura mínima 90 % en `services/`. Cubre *Cuadrícula de productos vendibles ordenada por más vendidos* → «Producto sin precio de venta», «Insumos y activos fuera», «Producto archivado fuera», «Producto de otra línea».
- [x] 4.3 Crear `actions/fair.ts` con `"use server"`: `registerDirectSale`, validando sesión, organización, rol y Zod antes de delegar en el servicio, con `revalidatePath` de las rutas de ingresos afectadas. **No revalida `/fair`**: la cuadrícula no depende de la venta recién hecha y esperar una revalidación rompería el criterio de retorno inmediato.
- [x] 4.4 Añadir el filtro `kind = 'order'` como invariante explícita y comentada de `services/orders/order-service.ts` —hoy está, pero sin requisito detrás— y ampliar `order-service.test.ts` para que quitarlo rompa una prueba. Cubre el delta de `orders` → *Vistas alternativas y filtros del tablero* → «La venta directa no aparece en el tablero» en su parte de servicio.

## 5. Grupo de rutas y layout del modo feria

- [x] 5.1 Crear `app/(fair)/layout.tsx`: sin cabecera, sin barra inferior, sin menú lateral y sin botón flotante — el cascarón de `(app)` **no se monta**, no se oculta (design, decisión 7). Mantiene `AuthCheck`, `UserProvider` y `OrganizationProvider`, porque la sesión y la organización sí hacen falta.
- [x] 5.2 Crear `app/(fair)/fair/page.tsx` como cascarón delgado que compone el módulo de feature y pasa como props los datos obtenidos en servidor (línea activa, canales y catálogo vendible).
- [x] 5.3 Verificar que `middleware.ts` protege `/fair` como ruta autenticada y que la salida del modo apunta a `/quick`, la ruta que ya existe.
- [x] 5.4 Añadir en `app/sw.ts` la regla de **cascarón** de `/fair`: precachear su documento y sus recursos, sin datos de negocio (design, decisión 12). Es la única excepción a la regla de KAM-11 de no cachear rutas, y va comentada con su motivo — el dato lo sirve el snapshot, con su hora, no el HTML cacheado.

## 6. Pantalla V6 · Venta rápida

- [x] 6.1 Crear `features/fair/fair-session-store.ts` (Zustand): línea y canal de la sesión, hidratados desde el `fairSnapshot` de Dexie y persistidos ahí mismo, no en `localStorage` aparte (design, decisión 8).
- [x] 6.2 Crear `features/fair/fair-start.tsx`: el paso de inicio que preselecciona el primer canal por `position`, exige elegir línea cuando la activa es «Todas», **guarda el snapshot cuando hay red** y no vuelve a aparecer una vez resuelto (design, decisiones 9 y 12). Sin red y sin snapshot previo, explica que hay que abrir la feria una vez con señal.
- [x] 6.3 `features/fair/fair-start.test.tsx`. Cubre *Línea y canal se eligen una vez por feria, no en cada venta* → «Línea activa preseleccionada», «“Todas” exige elegir línea», «Canal preseleccionado», «Cambiar el canal al iniciar la feria», «Reabrir dentro de la misma feria». Cubre además *El modo feria abre sin red desde el catálogo capturado* → «Sin red y sin captura previa».
- [x] 6.4 Crear `features/fair/product-grid.tsx`: cuadrícula táctil con foto, nombre y precio; sustituto legible cuando no hay foto; objetivos alcanzables con el pulgar en 390 px y sin desplazamiento horizontal. Un toque agrega al carrito sin abrir ningún diálogo. Muestra siempre de cuándo es el catálogo cargado (design, decisión 12).
- [x] 6.5 `features/fair/product-grid.test.tsx`. Cubre *Cuadrícula de productos vendibles ordenada por más vendidos* → «Producto sin foto», «Sin desplazamiento horizontal»; y *Carrito y cobro en cuatro interacciones o menos* → «Tocar dos veces el mismo producto».
- [x] 6.6 Crear `features/fair/cart-store.ts` (Zustand, en memoria, sin persistir) sobre las funciones puras de `lib/fair/cart.ts`, y `features/fair/cart-bar.tsx`: barra inferior fija con unidades, total y *Cobrar*, deshabilitado con el carrito vacío.
- [x] 6.7 `features/fair/cart-bar.test.tsx`. Cubre *Carrito y cobro en cuatro interacciones o menos* → «El total sigue al carrito», «Cobrar con el carrito vacío», «Quitar una línea».
- [x] 6.8 Crear `features/fair/checkout-sheet.tsx`: hoja de cobro con monto propuesto igual al total y editable, método, cliente opcional con el buscador de contactos existente, y un solo control *Confirmar*.
- [x] 6.9 `features/fair/checkout-sheet.test.tsx`. Cubre *Carrito y cobro en cuatro interacciones o menos* → «Monto propuesto»; y *La venta y su cobro se registran en una sola operación* → «Cobro parcial», «Venta sin cobro» en su parte de interfaz.
- [x] 6.10 Crear `features/fair/exit-fair-mode.tsx`: el único control de navegación del modo, situado lejos de los controles de venta y visualmente distinto de *Cobrar* y *Confirmar*.
- [x] 6.11 `features/fair/exit-fair-mode.test.tsx`. Cubre *El modo feria no ofrece ningún elemento de navegación tocable salvo la salida* → «Salida explícita», «El control de salida no se confunde con el de cobro».
- [x] 6.12 Crear `features/fair/fair-screen.tsx` que compone el paso de inicio, la cuadrícula, la barra, la hoja y el control de salida; al confirmar, encola el sobre y **vuelve a la cuadrícula con el carrito vacío sin esperar respuesta**, con una señal breve que no interrumpe la venta siguiente (design, decisión 6).
- [x] 6.13 `features/fair/fair-screen.test.tsx`. Cubre *Vuelta inmediata a la cuadrícula tras cada venta* → «Retorno sin pantallas intermedias», «No se espera al servidor», «Venta siguiente inmediata»; y *Línea y canal se eligen una vez por feria* → «El canal no se pregunta durante la venta».

## 7. Snapshot de feria, encolado y pendientes

- [x] 7.1 Añadir la tabla `fairSnapshot` al esquema Dexie de `lib/offline/db.ts` subiendo `OUTBOX_SCHEMA_VERSION` según su convención, y crear `lib/fair/snapshot.ts` con `saveSnapshot`, `readSnapshot` y `snapshotAge` — catálogo vendible, línea, canal e instante de captura (design, decisiones 8 y 12).
- [x] 7.2 `lib/fair/snapshot.test.ts`. Cubre *El modo feria abre sin red desde el catálogo capturado* → «Entrar con red captura el catálogo», «La antigüedad del catálogo está a la vista», «Volver a entrar con red renueva la captura», «Un cambio hecho sin señal no altera la captura».
- [x] 7.3 Registrar `directSale.create` en `features/sync/operations.ts` con su `send` —la Server Action de la tarea 4.3— y su `describe` en español y sin jerga, junto a los de pedidos. **No se toca el motor de la cola** (design, decisión 5).
- [x] 7.4 Montar `<SyncProvider />` en `app/(fair)/layout.tsx`. Es headless: registra las operaciones, refleja la cola en el store y dispara el vaciado al reconectar y cada 30 s, sin aportar ningún elemento de navegación (design, decisiones 5 y 7).
- [x] 7.5 Crear `features/fair/sync/capture-sale.ts`: llama a `capture()` con `{recordId: uuid de la venta, operation: DIRECT_SALE_CREATE, payload: el sobre, organizationId, userId}` y **`deadlineMs: 0`** (design, decisión 6). Un solo camino, con red y sin ella: no consultar `navigator.onLine` para decidir la vía.
- [x] 7.6 `features/fair/sync/capture-sale.test.ts` con la cola simulada: comprobar que **no espera** al vaciado ni con red disponible, que el `occurredAt` viaja dentro del `payload`, y que `recordId` es el `uuid` de la venta. Cubre *Vender sin conexión no falla ni duplica* → «Reintento por fallo de red», «La aplicación se cierra con ventas pendientes»; y *Vuelta inmediata a la cuadrícula tras cada venta* → «No se espera al servidor».
- [x] 7.7 Crear `features/fair/sync/pending-sales-indicator.tsx` sobre `useSyncStore`, filtrando `items` por `entry.operation === DIRECT_SALE_CREATE`, con reintentar y descartar mediante `retryEntry` / `discardEntry`, fuera del área de los controles de venta (design, decisión 13).
- [x] 7.8 `features/fair/sync/pending-sales-indicator.test.tsx`. Cubre *Indicador de ventas pendientes de sincronizar* → «Sube al vender sin conexión», «Llega a cero al sincronizar», «Sin pendientes», «Solo cuenta ventas», «Reintentar y descartar desde la feria»; y *Vender sin conexión no falla ni duplica* → «Fallo permanente visible».

## 8. Pruebas de integración

- [x] 8.1 `tests/integration/direct-sale-revenue.test.ts`: los ingresos de una línea con un pedido y una venta directa salen de `order_totals` como una sola fuente. Cubre el delta de `orders` → *Las consultas de ingresos incluyen las ventas directas* → los cuatro escenarios.
- [x] 8.2 `tests/integration/board-excludes-direct-sales.test.ts`: registrar una venta directa y comprobar que la consulta del tablero no la devuelve en ninguna de sus tres vistas ni con "Ver archivados" activado. Cubre el delta de `orders` → *Vistas alternativas y filtros del tablero* → «La venta directa no aparece en el tablero», «La venta directa tampoco aparece en lista ni calendario», «Ver archivados no la trae de vuelta».

## 9. e2e — `fair-offline.spec.ts`, la prueba crítica del proyecto

- [x] 9.1 Crear `tests/e2e/fair-offline.spec.ts` con su ayudante de sesión y una organización propia, entrando al modo feria desde `/quick`.
- [x] 9.2 Recorrido mínimo con red: dos productos, *Cobrar*, *Confirmar*, contando las interacciones. Cubre *Carrito y cobro en cuatro interacciones o menos* → «Venta de dos productos en cuatro interacciones», «Precio del momento».
- [x] 9.3 Ausencia de navegación: contar los elementos de navegación presentes **en el DOM** del modo feria, no solo los visibles (design, decisión 7). Cubre *El modo feria no ofrece ningún elemento de navegación tocable salvo la salida* → «Ningún elemento de navegación», «Entrada explícita».
- [x] 9.4 Retorno inmediato: medir el tiempo entre *Confirmar* y la cuadrícula lista, con umbral holgado y documentado en la prueba (design, Risks). Cubre *Vuelta inmediata a la cuadrícula tras cada venta* → «Retorno sin pantallas intermedias».
- [x] 9.5 Veinte ventas con `context.setOffline(true)` de Playwright —red del navegador, no estado simulado en la aplicación (design, decisión 10)—, comparando el tiempo de la primera con el de la vigésima contra el umbral documentado. Cubre *Vender sin conexión no falla ni duplica* → «Veinte ventas sin red».
- [x] 9.6 Reconexión: `setOffline(false)`, esperar a que el indicador llegue a cero y comprobar en la base **exactamente veinte** ventas, cada una con sus líneas, su cobro, su `occurred_at` real y ninguna repetida. Cubre *Vender sin conexión no falla ni duplica* → «Reconexión sin duplicados»; y *Indicador de ventas pendientes de sincronizar* → «Llega a cero al sincronizar».
- [x] 9.7 Cierre y reapertura con ventas pendientes, y salida del modo feria. Cubre *Vender sin conexión no falla ni duplica* → «La aplicación se cierra con ventas pendientes»; y *El modo feria no ofrece ningún elemento de navegación tocable salvo la salida* → «Salida explícita».
- [x] 9.8 Arranque en frío sin red: entrar al modo feria con red, cerrar, `setOffline(true)`, reabrir y comprobar que aparece la cuadrícula con su hora de captura y se puede vender. Cubre *El modo feria abre sin red desde el catálogo capturado* → «Abrir sin red tras haber entrado con red».
- [x] 9.9 Recorrido del ayudante: entra al modo feria, registra una venta con su cobro y la venta queda a su nombre. Cubre *Aislamiento y roles en el modo feria* → «El ayudante vende».

## 10. Cierre

- [x] 10.0 Confirmar que los escenarios que los dos requisitos `MODIFIED` de `orders` arrastran sin cambios —«Nace en el estado inicial de su línea», «Renombrar el estado inicial no cambia el comportamiento», «La base rechaza el alta sin líneas», «Alta registrada en la bitácora», «Pedido archivado oculto por defecto», «Ver archivados», «Vista de calendario», «Los filtros sobreviven al cambio de vista»— **siguen cubiertos por sus pruebas de KAM-07 y KAM-08 y siguen pasando**. Son contenido copiado para no perder detalle al archivar, no comportamiento nuevo: no se duplican pruebas, se verifica que las existentes no se rompieron.
- [x] 10.1 Verificar que **ningún escenario** de `specs/fair-mode/spec.md` y `specs/orders/spec.md` quedó sin una tarea de prueba que lo nombre (convención nº 12).
- [x] 10.2 Ejecutar la cadena completa de CI: `lint → typecheck → test:unit → supabase start → test:integration → build → test:e2e`. — *Toda verde. En la e2e completa quedan inestables bajo carga tres pruebas ajenas a este cambio (`archive-restore`, `status-config`, `order-board`, `order-entry`), que pasan en aislamiento; no las provoca KAM-12 —usan nombres únicos y no dependen de lo añadido a la semilla— pero conviene mirarlas.*
- [x] 10.3 Confirmar que no se almacenó ningún valor derivado en ninguna columna ni en ningún store, y que no se introdujo ningún concepto ausente del modelo conceptual (convenciones nº 4 y nº 11). — *La migración no añade ninguna columna. El total, el cobrado y el saldo siguen derivándose de `order_totals`; el orden de la cuadrícula, de `best_selling_products`. **Matiz registrado:** el `fairSnapshot` de Dexie guarda `quantitySold`, que es un derivado. Es una **captura explícita y fechada** para poder vender sin señal (decisión 12), no una fuente de verdad: se renueva al entrar con red y la pantalla muestra siempre de cuándo es. Ningún cálculo lee de ahí con red disponible.*
- [x] 10.4 Ejecutar `openspec validate kam-12-fair-mode-quick-sale --strict` y archivar el cambio. — *Validación en verde y cambio archivado el 2026-09-04. Los deltas ya estaban sincronizados: los 14 requisitos de `fair-mode` y `orders` figuraban en los specs principales, así que archivar no tocó ninguno.*
