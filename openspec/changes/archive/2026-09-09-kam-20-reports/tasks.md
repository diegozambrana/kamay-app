> Cada tarea de prueba nombra los escenarios del delta spec que cubre (convención nº 12: ningún escenario sin prueba referenciada). Los escenarios viven en `specs/reports/spec.md`, `specs/shared-expense-allocation/spec.md`, `specs/org-configuration/spec.md`, `specs/expenses/spec.md`, `specs/payments/spec.md` y `specs/orders/spec.md` de este cambio. Las decisiones citadas (D1–D10) son las de `design.md`.

## 1. Periodo y contrato de lectura

- [x] 1.1 Crear `lib/reports/period.ts` con los cinco atajos —este mes, mes anterior, últimos 3 meses, este año, rango libre— resueltos a un `{from, to}` civil en la zona horaria de la organización, reutilizando `startOfDayInTimezone` y `startOfNextDayInTimezone` de `lib/expenses/period.ts` en vez de escribir una segunda forma de cortar un día (D1). El rango invertido se rechaza aquí, antes de tocar la base.
- [x] 1.2 Definir en `types/` el contrato común de las cinco lecturas: un solo `ReportPeriod` y un solo `ReportScope` (periodo + línea o "todas"), de modo que ningún informe pueda recibir un rango propio (D1).
- [x] 1.3 `lib/reports/period.test.ts`: cubre *Un solo periodo gobierna los cinco informes* → «El corte respeta la zona horaria de la organización», «Rango invertido»; incluye una fecha límite de mes que cae en meses distintos según la zona horaria y el atajo de doce meses que exige el presupuesto.

## 2. Derivados en la base de datos

- [x] 2.1 Crear la migración `supabase/migrations/YYYYMMDDHHMMSS_reports.sql` (archivo nuevo; ninguna existente se toca, convención nº 6) con la cabecera de decisiones y la referencia a este cambio, siguiendo el formato de `20260907070311_dashboard_cash_flow.sql`.
- [x] 2.2 Añadir `cash_flow_by_line_range(p_organization_id, p_from, p_to, p_business_line_id)` — `returns table`, `language sql`, `security invoker` — como generalización de `cash_flow_by_line_month` a un rango arbitrario, **agregando sobre `line_cash_movements` (KAM-19)** en vez de repetir por tercera vez el `union all` de dos ramas (D2). **No tocar la vista mensual**: sigue sirviendo al panel.
- [x] 2.3 Añadir `report_expense_breakdown(...)`: egresos del rango agrupados por categoría y línea, distinguiendo `kind = 'purchase'` de `kind = 'expense'`, con las compras bajo una entrada propia identificable y sin atribuirlas a ninguna categoría real. Excluye archivados. Se apoya en `expense_totals`, no rehace el total del egreso.
- [x] 2.4 Añadir `report_product_ranking(...)`: unidades, ingresos, costo atribuido, margen y canal predominante por ítem en el rango, incluyendo ventas directas con el mismo criterio con el que `orders` ya las incluye, y con el precio registrado en la línea, no el del catálogo de hoy. El costo atribuido es el egreso asignado al pedido **prorrateado entre sus ítems por ingreso** (D6); cero cuando el pedido no tiene egreso asignado.
- [x] 2.5 Añadir `report_profitability(...)`: por pedido y por ítem, con ingresos, costo de materiales, margen y porcentaje. El costo son **únicamente los egresos asignados al pedido** por su total derivado (D6); los consumos de inventario no entran y la migración explica por qué, para que nadie lo lea como un olvido. Devuelve la marca **sin costo registrado** como columna, no como cálculo del cliente, para que se pueda filtrar por ella.
- [x] 2.6 Añadir `report_low_stock(...)`: insumos bajo mínimo con saldo, mínimo, faltante, último costo y proveedor habitual, apoyándose en el `below_min` que `item_balances` (KAM-18) ya deriva y en `item_last_cost`. **Sin parámetro de rango**: es el saldo de hoy (requisito *Informe de insumos por acabarse*). Los insumos sin mínimo declarado no aparecen.
- [x] 2.7 Añadir `report_line_comparison(...)`: ingresos y egresos propios por línea en el rango, más el total de egresos de la línea compartida en su propia fila identificable. **No reparte** (D4). Devuelve fila con ceros para la línea sin movimiento.
- [x] 2.8 Añadir a cada función el `where is_owner(p_organization_id)` **dentro** de la definición, con el comentario que explica por qué `security invoker` no basta: el ayudante lee cobros fila a fila desde KAM-10 (D3). `revoke execute ... from public` y `grant execute ... to authenticated`, siguiendo el patrón de `create_direct_sale`.
- [x] 2.9 Añadir en la misma migración los índices de D10: `expenses (organization_id, occurred_at)`, `orders (organization_id, occurred_at)` y `order_items (organization_id, item_id)`. `payments (organization_id, occurred_at)` ya existe desde KAM-14; no duplicarlo.
- [x] 2.10 Dejar escrito en la migración que `best_selling_products` **no se toca** y por qué su ventana de 90 días es deliberada, para que nadie "arregle" uno mirando el otro (riesgo declarado en design).
- [x] 2.11 `supabase/tests/reports_cash_flow_range.test.sql` (pgTAP): cubre *Flujo de caja del periodo por línea* → «Cobrado y pagado del mes», «Un rango que no empieza el día 1», «Los extremos del rango se incluyen», «Cada movimiento cae en el mes en que ocurrió», «El movimiento anulado no suma», «La línea la pone el destino», «Un mes sin movimiento no inventa filas». Cada prueba crea su propia organización.
- [x] 2.12 `supabase/tests/reports_expense_breakdown.test.sql` (pgTAP): cubre *Agregación de egresos por categoría y línea dentro de un periodo* → «Agrupación por categoría», «Las compras van aparte», «La suma cuadra con los egresos», «El egreso archivado no suma», «Un rango que no empieza el día 1».
- [x] 2.13 `supabase/tests/reports_product_ranking.test.sql` (pgTAP): cubre *Ranking de productos vendidos dentro de un periodo, con unidades y margen* → «Unidades y margen en la misma fila», «Las ventas directas cuentan», «El precio es el que se registró», «Lo archivado no suma», «Un rango que no empieza el día 1», «El costo se prorratea por ingreso dentro del pedido», «Un pedido sin egreso asignado no atribuye costo», «El ranking de feria no cambia» —comprobando que `best_selling_products` devuelve lo mismo antes y después de la migración—.
- [x] 2.14 `supabase/tests/reports_access.test.sql` (pgTAP): cubre el recorte por rol y el aislamiento en **las seis funciones** — *Flujo de caja del periodo por línea* → «El ayudante obtiene cero filas», «Ninguna organización ve a otra»; *Agregación de egresos…* → «El ayudante obtiene cero filas», «Ninguna organización ve a otra»; *Ranking de productos…* → «El ayudante obtiene cero filas»; *Reportes es una página completa reservada a la persona dueña* → «El ayudante consulta el derivado a mano». Distinguir cero filas por recorte de cero filas por ausencia de datos.
- [x] 2.15 `supabase/tests/reports_allocation_readonly.test.sql` (pgTAP): cubre *El reparto se aplica en la lectura y jamás reescribe el egreso* → «El egreso no se mueve de línea», «El reparto no crea filas nuevas», contando filas de `expenses` y comprobando `business_line_id` antes y después de leer el comparativo.
- [x] 2.16 Ejecutar `supabase db reset` y `supabase test db`, y regenerar el grafo con `graphify update .` (convención nº 6).

## 3. Regla de reparto

- [x] 3.1 Definir en `types/` la forma de la regla en `organizations.settings`: `{ rule: 'revenue' | 'equal' | 'manual', shares?: Record<lineId, number> }`, con su esquema Zod y el valor por defecto `revenue` para la organización que no la tiene declarada.
- [x] 3.2 Crear `lib/reports/allocation.ts` con **la única** implementación del reparto: recibe las filas de `report_line_comparison` y la regla, y devuelve las cifras repartidas **junto con la leyenda ya compuesta** —regla y proporciones resultantes— en el mismo objeto, de modo que tener las cifras sea tener la leyenda (D4).
- [x] 3.3 Implementar las tres reglas en ese módulo: proporcional a ingresos, con caída a partes iguales y leyenda explícita cuando los ingresos del periodo son cero; partes iguales sobre las líneas no compartidas y no archivadas; y manual con los porcentajes declarados, tratando como 0 la línea sin porcentaje.
- [x] 3.4 Implementar el cierre por resto mayor (D5): truncar a dos decimales y sumar la diferencia a la línea de mayor importe repartido, de modo que la suma sea exactamente el total. Contar los gastos compartidos una sola vez: repartidos y no además como línea propia.
- [x] 3.5 `lib/reports/allocation.test.ts`: cubre *Reparto proporcional a los ingresos* → «Reparto proporcional con ingresos desiguales», «Ninguna línea tuvo ingresos en el periodo», «Una sola línea activa»; *Reparto a partes iguales* → «Tres líneas activas», «Una línea archivada no participa»; *Reparto manual con porcentajes que suman 100* → «Porcentajes declarados», «Una línea nueva bajo regla manual»; *El reparto no altera el total* → «Redondeo que no cuadra», «Sin doble conteo»; y *Ningún resultado repartido se muestra sin su regla* → «Leyenda en el comparativo», comprobando que la leyenda sale del mismo objeto que las cifras.
- [x] 3.6 Añadir a `services/configuration/` la lectura y la escritura de la regla sobre `organizations.settings`, con la validación de suma 100 y el rechazo de porcentajes negativos y de reglas desconocidas en la Server Action correspondiente. La escritura queda restringida al dueño por la RLS que ya tiene `organizations`.
- [x] 3.7 `services/configuration/allocation-rule.test.ts`: cubre *La organización elige una regla de reparto entre tres* → «Valor por defecto de una organización nueva», «Una regla desconocida se rechaza»; *Reparto manual con porcentajes que suman 100* → «Los porcentajes no suman 100», incluido el rechazo del porcentaje negativo; y *General settings of the organization are editable* → «Manual percentages must add up».
- [x] 3.8 `supabase/tests/allocation_rule_access.test.sql` (pgTAP): cubre *General settings of the organization are editable* → «The assistant cannot change the rule», comprobando que la escritura del ayudante sobre `settings` se rechaza y el valor guardado no cambia.

## 4. Configuración de la regla en V15

- [x] 4.1 Añadir a `/settings/general` el bloque **Reparto de gastos compartidos**: selección entre las tres reglas y, solo para la manual, un porcentaje por línea no compartida con el total vivo a la vista.
- [x] 4.2 Mostrar el aviso de línea sin porcentaje declarado cuando la regla vigente es manual y existe una línea no compartida sin su parte, sin bloquear el guardado ni el alta de líneas (riesgo declarado en design).
- [x] 4.3 Confirmar que el guardado de la regla registra su evento en `activity_log` con los valores anterior y nuevo, como el resto de la sección General (convención nº 7).
- [x] 4.4 `features/settings/allocation-rule-form.test.tsx`: cubre *General settings of the organization are editable* → «Owner switches the allocation rule», «A line without a declared percentage»; y confirma que los dos escenarios heredados de KAM-04 —«Owner renames the organization», «General changes are logged»— siguen pasando en `features/settings/general-form.test.tsx` tras añadir el bloque.

## 5. Lectura de los informes

- [x] 5.1 Crear `services/reports/report-service.ts` con una lectura por informe, cada una llamando a su función de la base y **ninguna** sumando en JavaScript lo que la función ya suma (convención nº 1: nada de Supabase fuera de `services/`).
- [x] 5.2 Crear `lib/reports/margin.ts` con **la única** fórmula de margen y de porcentaje de margen, con el porcentaje vacío —no cero, no error— cuando los ingresos son cero. Es la misma fórmula que consumirá KAM-19; no escribirla dos veces.
- [x] 5.3 `lib/reports/margin.test.ts`: cubre *Informe de rentabilidad* → «Margen de un pedido con costo», «Pedido sin costo registrado», «Margen negativo», «Ingresos cero».
- [x] 5.8 `supabase/tests/reports_profitability.test.sql` (pgTAP): cubre *Informe de rentabilidad* → «Un consumo de inventario no cuenta como costo del pedido», registrando un consumo desde un pedido y comprobando que su costo sigue en cero y su marca intacta. Es la prueba que impide que alguien "arregle" D6 dentro de seis meses creyendo que es un fallo.
- [x] 5.4 `services/reports/report-service.test.ts`: cubre *El selector de línea filtra cuatro informes y el comparativo lo ignora* → «Cuatro informes se filtran», «El comparativo no se filtra», comprobando que el filtro de línea llega a la consulta y no se aplica después en memoria.
- [x] 5.5 `tests/integration/reports.test.ts`: cubre, contra cálculo directo sobre semilla conocida, *Informe de en qué se va el dinero* → «Desglose por categoría», «Las compras tienen su propia entrada», «Un periodo sin egresos»; *Informe comparativo entre líneas* → «Las tres líneas presentes», «Los gastos compartidos ya están dentro», «El total cuadra»; y *Un solo periodo gobierna los cinco informes* → «Las cifras cuadran entre informes», comparando el total de egresos de los dos informes sobre el mismo periodo.
- [x] 5.6 Ampliar `tests/integration/reports.test.ts` con *Informe de qué se vende más…* → «Las ventas de feria cuentan»; *Informe de rentabilidad* sobre datos sembrados; y *El reparto se aplica en la lectura…* → «Cambiar la regla recalcula lo pasado», leyendo el mismo periodo con dos reglas distintas.
- [x] 5.7 Ampliar `tests/integration/reports.test.ts` con *Informe de insumos por acabarse* → «Insumo bajo mínimo», «Insumo sin mínimo declarado»..

## 6. Pantalla V14

- [x] 6.1 Crear `app/(app)/reports/page.tsx`: guardián con `getOwnerContext` y redirección del ayudante antes del primer render, lectura de `searchParams` (`preset`, `from`, `to`, `line`), resolución **única** del periodo (D1) y composición de los cinco informes, cada uno en su propio límite de suspensión para que uno que falle no tumbe la pantalla (riesgo declarado en design).
- [x] 6.2 Crear `features/reports/report-header.tsx`: selector de periodo con los cinco atajos y selector de línea con "Todas", ambos escribiendo en la dirección y **sin** tocar la cookie de línea activa (D9).
- [x] 6.3 Crear `features/reports/profitability-report.tsx`: conmutador por pedido / por producto, tabla ordenable, margen negativo mostrado tal cual, y la marca **sin costo registrado** visible en la fila **y con filtro propio** (D6): cubre *Informe de rentabilidad* → «Filtrar los pedidos sin costo capturado».
- [x] 6.4 Crear `features/reports/expense-breakdown-report.tsx`: desglose por categoría y línea con peso relativo, compras como entrada propia, orden por importe descendente por defecto y por nombre a petición.
- [x] 6.5 Crear `features/reports/product-ranking-report.tsx`: ranking con unidades y margen **siempre visibles a la vez**, ordenable por cualquiera de las dos, con el canal predominante.
- [x] 6.6 Crear `features/reports/low-stock-report.tsx`: saldo, mínimo, faltante, último costo y proveedor, con la leyenda de que muestra el saldo de hoy y no el del periodo (D9), y la acción **crear tarea de reposición** que abre V18 prellenada con el insumo, su faltante y su línea..
- [x] 6.7 Crear `features/reports/line-comparison-report.tsx`: todas las líneas no archivadas con ingresos, egresos y margen ya repartidos, fila de total, la leyenda de la regla junto al resultado y el aviso de que este informe ignora el selector de línea (D9).
- [x] 6.8 Añadir `shadcn add chart` y montar el gráfico de cada informe con carga dinámica sin SSR, fuera del camino crítico (D7). La tabla es la fuente y se pinta sin esperarlo.
- [x] 6.9 Hacer que toda fila abra su registro: pedido a V4, ítem a V11, egreso a V7; y que una fila agregada abra el listado ya filtrado por el mismo criterio y periodo con el que se compuso.
- [x] 6.10 Componer la variante estrecha: un informe por pantalla con los selectores arriba y un conmutador entre los cinco, con las tablas legibles y ordenables y la exportación disponible.
- [x] 6.11 Añadir la entrada *Reportes* a la barra superior de escritorio y al menú *Más* del móvil, filtrada por rol, y confirmar que no se rinde para el ayudante.
- [x] 6.12 `features/reports/report-header.test.tsx`: cubre *Un solo periodo gobierna los cinco informes* → «Cambiar el periodo recalcula todo»; y *El selector de línea filtra cuatro informes y el comparativo lo ignora* → «La selección no se propaga», comprobando que no se escribe la cookie de línea.
- [x] 6.13 `features/reports/product-ranking-report.test.tsx`: cubre *Informe de qué se vende más…* → «Ordenar por unidades», «Ordenar por margen», «El que vende mucho y deja poco», comprobando que ambas columnas siguen presentes en las dos ordenaciones.
- [x] 6.14 `features/reports/line-comparison-report.test.tsx` y `features/reports/low-stock-report.test.tsx`: cubren *Ningún resultado repartido se muestra sin su regla* → «Un informe sin reparto no lleva leyenda», comprobando que el comparativo la lleva y el de insumos no.

## 7. Exportación

- [x] 7.1 Crear `lib/reports/csv.ts`: serialización a CSV con separador de coma, comillas RFC 4180 y UTF-8 con BOM, con las cifras como números sin formato de moneda (D8). Sin dependencia nueva.
- [x] 7.2 Componer la cabecera de contexto del archivo —periodo, línea y, cuando corresponda, leyenda de reparto— en el mismo módulo que produce el archivo, no en el componente que dibuja la tabla (D8).
- [x] 7.3 Crear la Route Handler de exportación, que vuelve a ejecutar la misma lectura con los mismos parámetros de la dirección en vez de serializar lo que quedó pintado en el cliente (D8), con el nombre de archivo que identifica informe y periodo, y con el mismo guardián de dueño de 6.1.
- [x] 7.4 `lib/reports/csv.test.ts`: cubre *Exportación de cualquier informe con sus filtros y su contexto* → «Cifras utilizables», «El archivo se explica solo»; y *Ningún resultado repartido se muestra sin su regla* → «Leyenda en la exportación». Incluye un valor con coma, uno con comillas y uno con acentos.

## 8. Pruebas de extremo a extremo y presupuesto

- [x] 8.1 `tests/e2e/reports.spec.ts`: cubre *Un solo periodo gobierna los cinco informes* → «El enlace reproduce el recorte»; *Reportes es una página completa reservada a la persona dueña* → «La dueña entra por dirección directa»; y *Cada fila abre su registro de origen* → «Fila de un pedido», «Fila de una categoría de gasto».
- [x] 8.2 Ampliar `tests/e2e/reports.spec.ts` con la exportación: cubre *Exportación de cualquier informe con sus filtros y su contexto* → «Exporta lo que se ve», descargando el archivo del ranking ordenado por margen y filtrado por una línea.
- [x] 8.3 Ampliar `tests/e2e/assistant-permissions.spec.ts` con: *Reportes es una página completa reservada a la persona dueña* → «El ayudante no ve la entrada», «El ayudante escribe la dirección».
- [x] 8.4 Ampliar `tests/e2e/reports.spec.ts` con la vista estrecha: cubre *Los informes se adaptan al móvil* → «Un informe por pantalla», a 390 px.
- [x] 8.5 Ampliar `tests/e2e/reports.spec.ts` con *Cada fila abre su registro de origen* → «Fila de un insumo», y con *Informe de insumos por acabarse* → «El periodo no lo recorta», «Tarea de reposición»..
- [x] 8.6 Medir el presupuesto sobre la semilla de doce meses —que existe desde KAM-14 como la organización aparte «Kamay Histórico», no como una ampliación de Geeko Store, cuyas filas son fixtures de otras suites—: cubre *La pantalla responde dentro de su presupuesto* → «Doce meses de datos», sobre la carga completa de la ruta y no sobre una pintura parcial. Revisar el plan de ejecución de cada función **antes** de añadir cualquier índice más allá de los de 2.9 (D10).

## 9. Cierre

- [x] 9.1 Ejecutar la secuencia completa: `lint → typecheck → test:unit → supabase start → test:integration → build → test:e2e`, con la cobertura de `lib/` y `services/` en 90 % o más.
- [x] 9.2 Regenerar el grafo con `graphify update .` y confirmar que `graphify-out/` queda versionado con el cambio.
- [x] 9.3 Revisar que ningún escenario de los seis delta specs quedó sin prueba referenciada.
