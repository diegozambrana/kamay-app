## 1. Documentos de producto (convención 11: primero el modelo, después el código)

- [x] 1.1 `specs/PRD/kamay-especificacion-producto-v6.md` §3: agregar «3.4 Administrador de la plataforma» (no pertenece a ninguna organización, ve y administra todas, entra a cualquiera como dueño, lo concede solo el operador, sus cambios quedan marcados en la bitácora de cada organización).
- [x] 1.2 `specs/PRD/kamay-esquema-base-de-datos-supabase.md` §5: tabla `platform_admins`, `is_platform_admin()`, `has_active_membership()` y los nuevos cuerpos de `is_member`/`is_owner` con la advertencia de que ya no significan «pertenece» (design D1, D2); §6: `create_organization()`; §16 matriz de acceso: columna «Administrador de la plataforma» = «Todo, como dueño»; §14: la marca `actor_label` del super admin (D5); §20: la excepción de `platform_admins` en la lista de verificación.
- [x] 1.3 `specs/PRD/ARCHITECTURE.md`: el grupo de rutas `(platform)` y `components/layout/app-shell.tsx` (D7); ampliar la regla del service role con «y en herramientas del operador que corren fuera de la aplicación (`scripts/`)» (D10).
- [x] 1.4 `specs/PRD/kamay-mapa-navegacion-ui.md`: las vistas *Organizaciones* y *Usuarios* (con sus detalles), el selector de organización del menú lateral y el cascarón reducido sin organización.

## 2. Base de datos

- [x] 2.1 Migración nueva `supabase/migrations/<timestamp>_platform_admins.sql`: tabla `platform_admins` con RLS, política de lectura de la fila propia, `revoke` a `anon`/`authenticated` salvo ese `select` y `grant select, insert, update` a `service_role`; `is_platform_admin()` (`security definer`, `search_path = ''`, `grant execute` a `authenticated`) (D1).
- [x] 2.2 En la misma migración: `has_active_membership(org)` con el cuerpo actual de `is_member`, y `create or replace` de `is_member`/`is_owner` con `or is_platform_admin()` y un comentario que explique el cambio de significado (D2).
- [x] 2.3 En la misma migración: `platform_list_users(p_organization_id uuid default null)` con la compuerta `insufficient_privilege`, `revoke` de `public`/`anon` y `grant` a `authenticated` (D3).
- [x] 2.4 En la misma migración: `create_organization(p_name, p_currency, p_timezone)` `security invoker` que comprueba `is_platform_admin()`, rechaza nombre vacío y siembra línea compartida *General* y los cinco estados mínimos (D4).
- [x] 2.5 En la misma migración: nueva versión de `log_activity()` a partir de la de `20260903210000_offline_sync.sql` que fija `actor_label = 'Administrador de la plataforma'` cuando `is_platform_admin() and not has_active_membership(v_org)` (D5).
- [x] 2.6 Recorrer el catálogo de funciones (`pg_proc`) buscando cuerpos que llamen a `is_member`/`is_owner` y anotar en el comentario de la migración, una por una, por qué el nuevo significado es correcto para cada una (o cambiarla a `has_active_membership` en la misma migración si no lo es).
- [x] 2.7 `supabase/tests/platform_admins.test.sql`: escenarios «An active row makes a platform admin», «A revoked row grants nothing», «No one promotes themselves» (dueña y super admin intentan `insert`/`update`, 4 argumentos en `throws_ok`), «Other admins are not listed through the table» y ninguna fila borrada con `DELETE`.
- [x] 2.8 `supabase/tests/platform_access.test.sql`: escenarios de `tenant-isolation` «A platform admin is owner everywhere», «A revoked platform admin is back to their memberships», «Strict membership ignores platform admin», y de `platform-administration` «Reads any organization» (pedidos, egresos y bitácora de B), «Writes as an owner would» (egreso en B), «Owner rules still apply» (`DELETE` sin efecto, archivar al último dueño rechazado), «Acting creates no membership» y «A platform admin who is an assistant somewhere is still an owner there».
- [x] 2.9 `supabase/tests/platform_functions.test.sql`: `platform_list_users` devuelve correos y membresías al super admin y lanza para una dueña («A non-admin cannot list accounts»); con `p_organization_id` solo cuentas de esa organización; `create_organization` crea la organización con una línea compartida y los estados por `kind` («The new organization is usable»), lanza para una dueña («Only a platform admin creates organizations») y con nombre vacío; todo-o-nada con un disparador temporal en `statuses` que falla para ese nombre («Creation is all-or-nothing»).
- [x] 2.10 `supabase/tests/platform_activity_mark.test.sql`: «A foreign change is marked» (edición de un pedido de B), «Creation is logged and marked», «A member's own change is not marked» (super admin dueño de A), y que la marca también aparece en cambios de membresía hechos desde el detalle.
- [x] 2.11 `supabase/tests/preproduction_checklist.test.sql`: sumar `platform_admins` a las exclusiones de «toda tabla tiene organization_id» (y de disparadores de auditoría si esa comprobación la alcanza) con su motivo escrito, y confirmar que sigue pasando (escenario «The catalog check names the exception»).
- [x] 2.12 `supabase test db` completo en verde con la semilla que ya incluye un super admin (escenario «The existing isolation suite still passes unchanged»), **sin editar** `rls_isolation.test.sql` ni `rls_roles.test.sql`, que siguen cubriendo «Active member is recognized», «Archived membership grants nothing», «Assistant is not owner», «Cross-organization reads return zero rows» y «Cross-organization writes are rejected» para cuentas que no son super admin; `graphify update .` tras la migración.

## 3. Operador y semilla

- [x] 3.1 `supabase/seed.sql`: `superadmin@kamay.test` (contraseña común) con su fila activa en `platform_admins` y ninguna membresía; agregar la cuenta a la tabla de usuarios de `docs/manual-de-pruebas.md`.
- [x] 3.2 `scripts/platform-admin.mjs` con `grant <email> [--note]`, `revoke <email>` y `list`; cliente propio con `NEXT_PUBLIC_SUPABASE_URL` y `SUPABASE_SERVICE_ROLE_KEY`; contraseña de cuenta nueva desde `PLATFORM_ADMIN_PASSWORD`, nunca como argumento (D10).
- [x] 3.3 `tests/integration/platform-admin-script.test.ts`: «The operator grants an existing account», «The operator grants an email with no account» (la cuenta nueva entra sin organización), «Revoking takes effect on the next request» (tras `revoke`, la misma sesión obtiene cero filas de B y la fila sigue con `archived_at`).
- [x] 3.4 Documentar el script en `supabase/README.md` (sección nueva «Administradores de la plataforma») y la recomendación de MFA en `docs/recuperacion.md`.

## 4. Acceso efectivo en el servidor

- [x] 4.1 `lib/auth/request-user.ts`: `getRequestPlatformAdmin()` con `cache()` sobre `rpc('is_platform_admin')` (D6).
- [x] 4.2 `lib/auth/session-context.ts`: `SessionContext` con `role`, `organization`, `membership | null` y `platformAdmin`; resolución de la organización para el super admin; `getOwnerContext()` compara `context.role`; nuevo `getPlatformAdminContext()` (D6, D9).
- [x] 4.3 `lib/auth/session-context.test.ts`: miembro normal igual que hoy; super admin sin membresía con cookie válida → rol `owner` y `membership` nula; super admin asistente en A → rol `owner`; super admin con cookie de organización archivada o ausente → `null`; cuenta revocada con cookie ajena → regla normal.
- [x] 4.4 Pasar todas las lecturas de `context.membership.role` a `context.role` y revisar cada uso de `context.membership` que `tsc` señale (acciones, páginas, `features/expenses/load-expense-detail.ts`, exportación).
- [x] 4.5 `lib/auth/post-auth.ts` y `app/auth/select-org/page.tsx`: el super admin sin organización válida va a `/admin/organizations` y nunca ve la selección; casos en `lib/auth/post-auth.test.ts` (escenarios «A platform admin without an organization lands on Organizations» y «A platform admin never gets the selection screen» a nivel unitario).
- [x] 4.6 `actions/auth.ts`: `enterOrganization(organizationId | null)` y la rama de super admin en `selectOrganization` (D8), con pruebas unitarias de la validación (organización inexistente o archivada → no fija cookie; `null` borra `kamay-org`).
- [x] 4.7 `lib/auth/routes.ts`: `/admin` en `PROTECTED_PREFIXES`, con su caso en `lib/auth/routes.test.ts`.
- [x] 4.8 `app/(fair)/layout.tsx`: misma resolución de acceso efectivo.

## 5. Cascarón y navegación

- [x] 5.1 Extraer `components/layout/app-shell.tsx` de `app/(app)/layout.tsx` (D7): providers, menú lateral, barra superior, barra de contexto, registro y barra móvil; con `organization = null`, cascarón reducido sin selector de línea, campana, registro ni barra de contexto.
- [x] 5.2 `app/(app)/layout.tsx`: sin membresías y sin super admin → aviso; super admin sin organización válida → `redirect("/admin/organizations")`; si no, `<AppShell>` con el acceso efectivo.
- [x] 5.3 `components/providers/user-provider.tsx` y `stores/user-store.ts`: `role`, `platformAdmin`, `membership | null`; `app-sidebar.tsx`, `mobile-nav.tsx`, `user-menu.tsx`, `quick-grid.tsx` y `register-button.tsx` leen `role`.
- [x] 5.4 `components/layout/nav-entries.ts`: entradas «Organizaciones» (`/admin/organizations`) y «Usuarios» (`/admin/users`) marcadas como de plataforma, siempre `mobile: "more"`; `navEntriesFor`/`moreEntriesFor` reciben si la cuenta es super admin y, sin organización, devuelven solo las de plataforma. Casos en `components/layout/nav-entries.test.ts`: escenarios «Desktop sidebar carries the platform entries», «Nobody else sees them» y que la barra inferior sigue con tres entradas más «Más».
- [x] 5.5 `features/platform/organization-switcher.tsx` (D8): `DropdownMenu` con filtro, organizaciones activas con la actual marcada y «Vista de plataforma»; icono con tooltip con el menú plegado. Montarlo en el `SidebarHeader` solo si `platformAdmin` y actualizar el comentario de `app-sidebar.tsx`.
- [x] 5.6 `features/platform/organization-switcher.test.tsx` y `components/layout/app-sidebar.test.tsx`: filtra por nombre, marca la activa, elegir llama a `enterOrganization` con el id, «Vista de plataforma» la llama con `null`; sin `platformAdmin` no se renderiza (escenario «A multi-organization owner has no selector»); plegado muestra el icono con tooltip («The selector survives the collapsed sidebar»).

## 6. Servicios y acciones de plataforma

- [x] 6.1 `services/platform/organization-admin-service.ts`: `listAll()` (dueños activos y cantidad de miembros activos calculados en la consulta, no almacenados), `get(id)`, `create()` por `rpc('create_organization')`, `update()` de nombre, moneda y zona horaria; pruebas con el cliente simulado (cobertura ≥ 90 %).
- [x] 6.2 `services/platform/user-admin-service.ts`: `list(filters)` y `listForOrganization(id)` sobre `rpc('platform_list_users')`, con el filtro «sin organización» y la búsqueda por correo o nombre como funciones puras probadas.
- [x] 6.3 `services/platform/membership-admin-service.ts` (D9): `assign()` con resultado por organización (`created | restored | already_member`), `setRole`, `setDisplayName`, `archive`, `restore`; nombre por defecto desde otra membresía o la parte local del correo. Pruebas: «Adding a former member restores them», «Adding a current member is rejected», «One failing assignment does not hide the others».
- [x] 6.4 `lib/platform/schema.ts` (Zod; en `lib/<dominio>/schema.ts` como el resto del proyecto): organización (nombre obligatorio, moneda, zona horaria con los valores por defecto del producto), asignación, invitación. Pruebas unitarias incluido «An empty name is rejected».
- [x] 6.5 `actions/platform.ts`: `createOrganization`, `updateOrganization`, `assignMemberships`, `inviteToOrganization` (reutiliza `InvitationService.create` y el armado del enlace de `actions/members.ts`), `setMembershipRole`, `setMembershipDisplayName`, `archiveMembership`, `restoreMembership`; todas detrás de `getPlatformAdminContext()`, con Zod y `revalidatePath`. Pruebas unitarias: cuenta sin super admin → error sin llamar al servicio.
- [x] 6.6 `tests/integration/platform-admin.test.ts` (sesión real del super admin sembrado, organizaciones propias creadas en la prueba): «Adding an existing account as owner», «Assigning two organizations at once», «The last owner stays», «Renaming a membership» (y su evento marcado).

## 7. Vista *Organizaciones*

- [x] 7.1 `app/(platform)/layout.tsx`: exige super admin (si no, redirige al aterrizaje por dispositivo) y monta `<AppShell>` con la organización activa si la hay (D7); `loading.tsx` y `error.tsx` por segmento nuevo.
- [x] 7.2 `app/(platform)/admin/organizations/page.tsx` + `features/platform/organizations/organization-list.tsx`: lista con nombre, dueños, miembros activos y fecha de creación, búsqueda por nombre y «Mostrar más» resueltos en el servidor (`?q=`, `?limit=`, spec `performance-budget`), «Entrar» y «Ver detalle»; estados vacío y sin resultados con los componentes de KAM-23.
- [x] 7.3 `features/platform/organizations/create-organization-dialog.tsx`: nombre, moneda y zona horaria; al crear navega al detalle.
- [x] 7.4 `app/(platform)/admin/organizations/[id]/page.tsx` + `features/platform/organizations/organization-detail.tsx`: edición de datos; miembros con correo, nombre, rol y estado; agregar cuenta existente por su correo (`addAccountByEmail`, design D9), invitar (muestra el enlace una vez), cambiar rol, quitar y restaurar acceso.
- [x] 7.5 Pruebas de componente: `organization-list.test.tsx` («Searching by name»), `create-organization-dialog.test.tsx` (nombre vacío no envía), `organization-detail.test.tsx` (lista con correo; «Adding a current member is rejected» muestra el mensaje).
- [x] 7.6 Confirmar que `app/route-states.test.tsx` pasa con los segmentos nuevos.

## 8. Vista *Usuarios*

- [x] 8.1 `app/(platform)/admin/users/page.tsx` + `features/platform/users/user-list.tsx`: correo, nombre, organizaciones con rol y estado, marca de super admin y último acceso; búsqueda, filtro «Sin organización» y «Mostrar más» resueltos por `platform_list_users()` (`?q=`, `?sin=1`, `?limit=`).
- [x] 8.2 `app/(platform)/admin/users/[id]/page.tsx` + `features/platform/users/user-detail.tsx`: asignar varias organizaciones con rol por cada una en un paso (muestra el resultado por organización); por membresía, cambiar rol, nombre visible y quitar/restaurar acceso; correo y condición de super admin solo lectura, sin control de contraseña.
- [x] 8.3 Pruebas de componente: `user-list.test.tsx` («Accounts without organization are found»), `user-detail.test.tsx` («Email and password are not editable», «No screen offers the grant»: la marca de super admin no tiene control).

## 9. Bitácora, perfil y equipo

- [x] 9.1 `lib/activity/describe.ts`: `actorOf()` prefiere la etiqueta cuando el evento trae autor y etiqueta (D5); constante `PLATFORM_ADMIN_LABEL` en `lib/platform/labels.ts` (la comparten bitácora y perfil). Casos en `lib/activity/describe.test.ts`: evento con `actor_id` + marca → «Administrador de la plataforma» aunque el nombre se resuelva; `'sistema'` sin cambios (escenario «The owner sees the mark» a nivel unitario).
- [x] 9.2 El perfil pasa a `app/(account)/profile/` (design D7) y su decisión a `features/account/profile-view.ts`: para el super admin, «Administrador de la plataforma» como acceso; sin membresía en la organización activa, sin edición de nombre; sin organización, sin nombre de organización. Prueba de componente con los escenarios «Profile inside a foreign organization» y «Profile without an active organization».
- [x] 9.3 Comprobar con pruebas que el super admin no entra en el equipo: la lista de *Usuarios y roles* y el selector de responsable de tareas salen de `memberships` (prueba unitaria del servicio que los alimenta: sin membresía no aparece — «Not listed among the members», «Not assignable»); `tests/integration/` sobre el generador de avisos con el super admin sembrado presente: ningún aviso dirigido a él («No notifications from foreign organizations»).

## 10. Consultas sin filtro de organización

- [x] 10.1 Recorrer `services/` buscando consultas a tablas con `organization_id` sin `.eq("organization_id", …)`; corregir las que dependían solo de RLS o dejar escrito por qué no lo necesitan (las que filtran por `user_id` propio, p. ej. `MembershipService.listActiveForUser`). Anotar el resultado en el PR.
  - Resultado (2026-09-14): cinco consultas sin `organization_id` en la cadena, ninguna mezcla organizaciones. `expense-service.ts:120,157,188` y `order-service.ts:120` filtran por `in(ids)` de egresos/pedidos que salen de una consulta ya acotada a la organización; `payment-service.ts:140` inserta una fila que trae su `organization_id`. `MembershipService.listActiveForUser` filtra por el `user_id` propio. Nada que corregir.

## 11. Pruebas e2e

- [x] 11.1 `tests/e2e/helpers/fresh-org.ts`: `createPlatformAdmin()` (cuenta propia con su fila en `platform_admins`, sin membresías) y la opción de darle membresías con `addOrganizationFor`.
- [x] 11.2 `tests/e2e/platform-admin.spec.ts`, bloque «entrada»: el super admin sembrado entra y aterriza en `/admin/organizations` sin aviso «sin organización» («A platform admin without memberships never sees the notice», «A platform admin without an organization lands on Organizations»); con dos membresías y sin organización, entrar o abrir `/auth/select-org` lleva a `/admin/organizations` («A platform admin never gets the selection screen»); tras elegir una organización, cerrar sesión y volver a entrar aterriza en `/dashboard` dentro de ella («A platform admin back in an organization lands on the home»).
- [x] 11.3 Bloque «cascarón sin organización»: `/orders` redirige a `/admin/organizations` («Organization-scoped routes need an organization»); en `/admin/users` el menú muestra «Organizaciones», «Usuarios» y el selector, hay menú de cuenta y no hay selector de línea, campana ni registro («The platform shell omits organization controls»).
- [x] 11.4 Bloque «Organizaciones»: la organización de una prueba (`createFreshOrganization`) aparece con su dueña («Every organization is listed»); buscarla por nombre; «Entrar» lleva a `/dashboard` con sus datos y las entradas de dueño («Entering an organization»); crear «Taller Norte <sufijo>» y aterrizar en su detalle; editar nombre y zona horaria («Editing the organization's data»); ver miembros con correo («Members are listed with their email»); invitar y ver el enlace («Inviting an email»).
- [x] 11.5 Bloque «Usuarios»: la cuenta de una prueba aparece con su organización y rol («Every account is listed»); filtro «Sin organización» con `createAccountWithoutOrganization()`; asignarle dos organizaciones con roles distintos y comprobar que entra a la primera como dueña.
- [x] 11.6 Bloque «selector»: dentro de A, elegir B en el menú lateral muestra el panel de B («Switching organization from the sidebar»); «Vista de plataforma» lleva a `/admin/organizations` sin organización activa («Leaving to the platform view»).
- [x] 11.7 Bloque «bitácora»: el super admin edita un pedido de una organización de prueba; la dueña de esa organización entra y ve «Administrador de la plataforma» como autor en la bitácora y en el historial del pedido («The owner sees the mark»).
- [x] 11.8 Bloque «revocación»: con `createPlatformAdmin()`, revocar con el script (o su función) y comprobar que la siguiente navegación ya no muestra la organización ajena ni `/admin/organizations`.
- [x] 11.9 Reservas: en `tests/e2e/assistant-permissions.spec.ts`, el ayudante abre `/admin/users` y `/admin/organizations` y es redirigido («An assistant cannot open the view»); una dueña de `createFreshOrganization` abre `/admin/organizations` y es redirigida («An owner cannot open it»); ni dueña ni ayudante ven las entradas de plataforma ni el selector, incluida la dueña con dos organizaciones («A multi-organization owner has no selector», «Nobody else sees them»).
- [x] 11.10 Proyecto móvil: el panel «Más» del super admin ofrece «Organizaciones» y «Usuarios» y la barra inferior sigue con cuatro ranuras («Mobile carries them in "Más"»); las cuatro pantallas de plataforma a 390 px sin desplazamiento horizontal de la página («Platform views at 390 px»); la auditoría de accesibilidad de KAM-23 incluye `/admin/organizations` y `/admin/users`.

## 13. Tablas y «Agregar usuario» (pedido durante la implementación)

- [x] 13.1 `components/shared/data-table.tsx` y `row-actions-menu.tsx`, adaptados de `CustomTable`/`TableActionsDropdown` de katu-ui sin búsqueda ni paginación en el cliente (design D12); `data-table.test.tsx`.
- [x] 13.2 *Organizaciones*, *Usuarios* y el equipo del detalle de organización, sobre `DataTable`, con menú de acciones por fila (ver detalle, entrar, quitar y devolver acceso) y tarjetas en el celular; pruebas de componente actualizadas.
- [x] 13.3 `addUserToOrganization` (reemplaza a `addAccountByEmail` e `inviteToOrganization`) y `features/platform/users/add-user-dialog.tsx`, en *Usuarios* y en el detalle de organización (organización fija). Pruebas: `add-user-dialog.test.tsx` y `actions/platform.test.ts` con los escenarios «Adding an existing account from the Users view», «Adding an email with no account invites it», «Adding someone who already belongs is reported» y «The organization's detail preselects the organization».
- [x] 13.4 e2e: `platform-admin.spec.ts` adaptada a las tablas (filas visibles, porque tabla y tarjetas conviven en el DOM) y bloque «agregar usuario desde Usuarios»; el detalle de organización agrega e invita desde el diálogo.

## 12. Verificación

- [x] 12.1 `npm run lint`, `npm run typecheck`, `npm run test:unit` (cobertura ≥ 90 % en `lib/` y `services/`) y `supabase test db` en verde.
- [x] 12.2 `npm run test:integration` en verde.
- [ ] 12.3 `npx playwright test tests/e2e/platform-admin.spec.ts tests/e2e/assistant-permissions.spec.ts tests/e2e/auth.spec.ts tests/e2e/account.spec.ts` en verde en escritorio y móvil —las pruebas existentes de `auth.spec.ts` y `account.spec.ts` siguen cubriendo, sin cambios, «Desktop lands on the dashboard», «Mobile lands on quick capture», «Multi-organization user selects before continuing», «Single-organization user skips selection» y los tres escenarios del aviso «sin organización»—, y la suite completa antes de fusionar (≈ 18 min).
  - Estado (2026-09-14): las cuatro specs nombradas, en verde en escritorio y móvil (105 pasadas, 30 omitidas por proyecto). **La suite completa no quedó en verde** contra el `next dev` compartido: 48 fallos en la primera corrida (con recargas en caliente de por medio), 29 al repetirlos y 10 con 2 workers, repartidos casi al azar entre suites ajenas a este cambio (tareas, inventario, egresos, pedidos). Los que se repiten (`assistant-permissions.spec.ts:639`, `order-flow.spec.ts:30`) pasan aislados con margen (10,6 s contra 30 s de límite). Falta una corrida de referencia de `main` en las mismas condiciones, o la de CI contra el build, antes de fusionar.
- [x] 12.4 Medir de nuevo el presupuesto de carga del panel con una dueña normal (`performance-budget`) y dejar el número en el PR (riesgo de D2).
  - Resultado (2026-09-14, servidor de desarrollo, móvil de gama media emulado, «Kamay Rendimiento»): 2563–3063 ms con las funciones nuevas y 2540–3043 ms con las originales restauradas, tres corridas cada una. Sin diferencia medible; los dos superan los 2 s porque el presupuesto se mide contra el build de producción en `stability.yml`, no contra `next dev`. En la base, `psql` sobre las consultas del panel: +0,1 a +2,5 ms por consulta (≈ 5–7 ms en total). El panel de escritorio (`carga en menos de 1,5 s`) pasa.
- [x] 12.5 Recorrido a mano con el servidor de desarrollo: `superadmin@kamay.test` entra, crea una organización, le agrega una dueña, entra con el selector, registra un pedido, sale a «Vista de plataforma»; captura de pantalla del menú lateral con el selector.
  - Hecho con un script de Playwright (el recorrido entra con contraseña, y eso no se teclea a mano en el navegador): entra, crea la organización, le agrega una dueña por correo, entra desde la lista, abre el selector y sale a «Vista de plataforma»; capturas de las cinco pantallas. El pedido no se registró por la interfaz en el recorrido: la escritura del super admin sobre un pedido ajeno la cubren la e2e «bitácora» y `platform_access.test.sql`. De paso aparecieron y se corrigieron dos cosas: Escape no cerraba el selector (el filtro detenía todas las teclas) y el aviso de React «Cannot update a component while rendering a different component» al pasar entre grupos de rutas (`components/providers/use-hydrate-store.ts`).
- [x] 12.6 `openspec validate kam-26-super-admin --strict` sin errores y `graphify update .` tras el último cambio de código.
