## 1. Documento de producto

- [x] 1.1 `specs/PRD/kamay-mapa-navegacion-ui.md` §2.3: sumar a la fila *Diálogo* «crear y editar entradas de configuración, e invitar (V15, V22)» y «confirmar archivar, restaurar, quitar acceso, revocar y las acciones del juego de estados (V15, V22)».

## 2. Diálogos compartidos

- [x] 2.1 Confirmar que `dialog`, `alert-dialog`, `dropdown-menu`, `table`, `select`, `checkbox`, `field` y `badge` están en `components/ui/`; si falta alguno, instalarlo con `npx shadcn@latest add <componente>` y anotarlo aquí (design D6).
- [x] 2.2 `components/shared/confirm-dialog.tsx`: `ConfirmDialog` presentacional sobre `AlertDialog` (botón de acción como `Button` propio, no `AlertDialogAction`; estilo destructivo opcional; `children` para contenido extra; error con `role="alert"` dentro) y `useConfirmDialog()` que devuelve `{ ask, dialog }`, corre la acción en `startTransition`, se queda abierto con `{ error }` y se cierra sin él; mientras corre, botones deshabilitados y Escape sin efecto (design D2).
- [x] 2.3 `components/shared/confirm-dialog.test.tsx`: la acción no corre al abrir; «Cancelar» y Escape cierran sin llamarla; confirmar la llama una vez y cierra; con `{ error }` el diálogo sigue abierto y muestra el error; el botón queda deshabilitado mientras corre.
- [x] 2.4 `components/shared/form-dialog.tsx`: `FormDialog` controlado (sin `DialogTrigger`) con `DialogHeader`, `<form>` con `id` propio, error con `role="alert"` dentro, y `DialogFooter` con «Cancelar» (`DialogClose`) y el botón de envío (`form={id}`, deshabilitado con `pending`) (design D3). Incluye `useEntityDialog<T>()` (abierto, registro, envío en curso y error) para no repetir ese estado en cada sección.
- [x] 2.5 `components/shared/form-dialog.test.tsx`: enviar entrega el `FormData` con los campos; «Cancelar» cierra sin enviar; con error el diálogo sigue abierto conservando lo escrito; al reabrir tras cancelar, los campos están vacíos (escenario «Cancelling creates nothing» a nivel de componente).
- [x] 2.6 Abrir un diálogo desde un ítem de `RowActionsMenu` devuelve el foco al «⋯» al cerrarlo: `components/shared/row-dialog-focus.test.tsx` con un `DataTable` + `FormDialog` y + `useConfirmDialog` (escenario «Focus returns to the row menu»). Falló como se esperaba; se resolvió en `hooks/use-return-focus.ts`, que ahora recuerda el disparador del menú cuando el foco estaba en un ítem (design D4).

## 3. Catálogos: Líneas, Canales, Categorías y Unidades

- [x] 3.1 `features/settings/config-list.tsx` → `ConfigTables<T>`: `DataTable` de activos (`${entity}-list` / `${entity}-row`) con «Editar» y «Archivar» (destructiva, omitida en filas `protected`); título «Archivados» y segunda `DataTable` (`${entity}-archived-list`) solo si hay archivados, con «Restaurar» como única acción; archivar y restaurar por `useConfirmDialog` con el texto de cada entidad; vacío inicial que remite al botón de alta de la sección (design D5).
- [x] 3.2 `features/settings/config-list.test.tsx` con `archiveConfigurationItem` / `unarchiveConfigurationItem` simuladas: menú de una fila con «Editar» y «Archivar» y sin botones sueltos («A line row offers its actions from the menu»); la fila protegida sin «Archivar» («The shared line is not offered for archiving in the interface»); elegir «Archivar» abre la confirmación sin llamar a la acción («Archiving a line asks first»); confirmar la llama y «Cancelar» no («Confirming archives», «Cancelling leaves the entry untouched»); «Restaurar» en un archivado pide confirmación y llama a `unarchiveConfigurationItem` («Restoring an archived entry asks first»); sin archivados no hay tabla «Archivados» («No archived entries, no archived table»); el «⋯» se abre con Enter y sus ítems se recorren con flechas («The menu is reachable by keyboard»).
- [x] 3.3 `features/settings/line-dialog.tsx`: `FormDialog` con *Nombre* y `Select` de *Color* (con su punto y `name="color"`); títulos «Nueva línea» / «Editar línea», botón «Crear línea» / «Guardar cambios».
- [x] 3.4 `features/settings/business-lines-section.tsx`: sin formulario en línea; botón «Crear línea» junto al título; estado `editing`; columnas nombre (con punto de color) y color; la compartida marcada `protected`.
- [x] 3.5 `features/settings/named-item-dialog.tsx` y `named-section.tsx`: los rótulos («Nuevo canal» / «Crear canal», «Nueva categoría» / «Crear categoría») salen de `ENTITY_COPY` en `config-list.tsx` según la `entity` que la página ya pasaba, así que `app/(app)/settings/channels/page.tsx` y `categories/page.tsx` no cambian.
- [x] 3.6 `features/settings/unit-dialog.tsx` y `units-section.tsx`: *Código* y *Nombre*; botón «Nueva unidad» / «Crear unidad»; columnas código y nombre.
- [x] 3.7 `features/settings/business-lines-section.test.tsx`, `named-section.test.tsx` y `units-section.test.tsx` con las acciones simuladas: la página no pinta ningún campo *Nombre* fuera del diálogo («No inline creation form»); «Crear línea» abre el diálogo con *Nombre*, *Color*, «Crear línea» y «Cancelar», y enviar llama a `createBusinessLine` con nombre y color y cierra («Owner creates a line from the dialog», nivel unitario); «Editar» abre el diálogo con los valores de la fila («The edit dialog opens prefilled») y «Guardar cambios» llama a `updateUnit` («Owner renames a unit», nivel unitario); la tabla de unidades muestra código y nombre con su «⋯» («A unit row shows its code and name»); con `{ error }` de `createSalesChannel` el diálogo sigue abierto con el nombre escrito y el error dentro («Duplicate name»); archivar un canal lo pasa a «Archivados» con solo «Restaurar» («An archived channel moves to the archived table», con filas re-renderizadas como las devolvería el servidor).

## 4. Usuarios y roles

- [x] 4.1 `features/settings/members/invite-dialog.tsx`: *Correo* y `Select` de *Rol*; al crear, el mismo diálogo muestra el enlace (`data-testid="invite-url"` con su `<code>`), «Copiar enlace» (`navigator.clipboard.writeText`), el aviso «No se vuelve a mostrar» y «Listo»; al cerrar se descarta (design D7).
- [x] 4.2 `features/settings/members/member-dialog.tsx`: `Select` de rol controlado; si es ayudante, líneas con `Checkbox` (`aria-label` «<línea> para <nombre>») y el texto de «ninguna marcada = todas»; al guardar, `changeMemberRole` si cambió el rol y `setMemberLines` si queda ayudante y cambiaron las líneas, en ese orden; error dentro del diálogo.
- [x] 4.3 `features/settings/members-section.tsx`: botón «Invitar» junto al título; `DataTable` *Equipo* (`member-list` / `member-row`: nombre, rol, líneas) con «Editar» y «Quitar acceso» (destructiva, confirmada); *Invitaciones pendientes* (`invitation-list` / `invitation-row`: correo, rol, vence) con «Revocar» (confirmada); *Sin acceso* sin acciones. Se retiran el `<select>` de rol y el `LinePicker` de la fila.
- [x] 4.4 `features/settings/members-section.test.tsx` con `actions/members` simuladas: invitar desde el diálogo muestra el enlace, el botón de copiar y el aviso («Owner invites and copies the link from the dialog», nivel unitario); editar un ayudante y marcar solo *Alfarería* llama a `setMemberLines` con esa línea y no a `changeMemberRole` («Owner restricts an assistant to one line», nivel unitario); cambiar el rol a dueño oculta las líneas y guardar no llama a `setMemberLines` («Promoting to owner hides the lines»); «Quitar acceso» + «Cancelar» no llama a `archiveMembership` («Removing access asks first»); con `{ error }` de `archiveMembership` la confirmación sigue abierta con el mensaje («The last owner cannot lose access», nivel unitario); «Revocar» confirmado llama a `revokeInvitation` («Revoking an invitation asks first»); la fila no tiene selector de rol ni casillas.

## 5. Estados

- [x] 5.1 `features/settings/statuses/status-dialog.tsx`: `FormDialog` con *Nombre*, `Select` de *Tipo* y de *Color*, `Checkbox` «Columna en cola» (`name="isQueue"`); valida con `statusFormSchema` y `setIsComplete` (con los hermanos) antes de enviar; «Agregar estado» / «Guardar cambios».
- [x] 5.2 `features/settings/statuses/archive-status-dialog.tsx`: sobre `ConfirmDialog`, con el `Select` «Mover los registros que lo usaban a» como contenido y el botón «Archivar estado»; si archivar deja el juego sin inicial o final, lo avisa y deshabilita el botón.
- [x] 5.3 `features/settings/statuses/status-row.tsx`: sin `mode`, formulario ni archivado propios; asa, punto, nombre, tipo, «Columna en cola» y `RowActionsMenu` («Acciones de <estado>») con «Editar» y «Archivar» que llaman a `onEdit` / `onArchive` (design D8).
- [x] 5.4 `features/settings/statuses/statuses-section.tsx`: monta `StatusDialog` (crear/editar) y `ArchiveStatusDialog`; «Restaurar valores por defecto», «Crear el juego por defecto», «Crear juego propio para esta línea» y «Usar el juego de la organización» por `useConfirmDialog` con su texto (design D8); se elimina el tipo `Confirming` y las confirmaciones en línea. Selector de flujo y de alcance sin cambios.
- [x] 5.5 Actualizar `features/settings/statuses/status-row.test.tsx`: el menú ofrece «Editar» y «Archivar» y la fila no tiene otros botones de acción («Las acciones de un estado están en su menú»); el asa sigue con su nombre accesible «Reordenar <estado>».
- [x] 5.6 `features/settings/statuses/statuses-section.test.tsx` con `actions/statuses` simuladas: «Editar» abre el diálogo con los valores y «Guardar cambios» llama a `updateStatus` y cierra («Editar un estado en un diálogo», nivel unitario); cambiar el tipo del único final deja el diálogo abierto con el aviso y no llama a `updateStatus` («El diálogo no deja un juego sin estado final»); «Archivar» abre la confirmación con el selector de destino y `archiveStatus` solo se llama al pulsar «Archivar estado» con el `moveToId` elegido («Archivar pide a dónde mover dentro de la confirmación»); «Restaurar valores por defecto» + «Cancelar» no llama a `restoreDefaultStatuses` («Cancelar la restauración no cambia nada»).

## 6. Secciones de un solo formulario

- [x] 6.1 `features/settings/retention-form.test.tsx`: cambiar los meses y pulsar «Guardar» llama a `updateRetentionPolicy` y muestra la confirmación en la página sin ningún `dialog` ni `alertdialog` («Saving retention does not ask for confirmation»). Verificar que `general-form.tsx`, `allocation-rule-form.tsx`, `notifications-section.tsx` y `export-section.tsx` no cambian.

## 7. Pruebas e2e

- [x] 7.1 Mover `visibleRows()` de `tests/e2e/platform-admin.spec.ts` a `tests/e2e/helpers/` y usarlo desde allí; añadir un ayudante para elegir una opción de un `Select` de shadcn (`combobox` → `option`).
- [x] 7.2 `tests/e2e/settings.spec.ts`: «la línea creada queda disponible…» crea desde el diálogo «Crear línea» eligiendo el color en el `Select` («Owner creates a line from the dialog», «New line appears in the selector with its color»); «la línea compartida no ofrece archivar» abre el «⋯» de *General* y comprueba «Editar» sin «Archivar» («The shared line is not offered for archiving in the interface»); «la línea archivada desaparece del selector» pasa por «⋯» → «Archivar» → confirmación, y la línea aparece en «Archivados» («Archiving a line asks first», «Confirming archives»).
- [x] 7.3 `tests/e2e/status-config.spec.ts`: renombrar por «⋯» → «Editar» → diálogo → «Guardar cambios» («Editar un estado en un diálogo», «Personalizar una línea sin afectar a las demás»); «Crear juego propio para esta línea» y «Usar el juego de la organización» confirmados en su diálogo («Volver al juego de la organización»); el arrastre sigue igual («Reordenar por arrastre»).
- [x] 7.4 `tests/e2e/invitation.spec.ts`: invitar desde el diálogo y leer el enlace de `invite-url` dentro del diálogo («Owner invites and copies the link from the dialog»).
- [x] 7.5 `tests/e2e/assistant-permissions.spec.ts` (restringir y devolver líneas al ayudante): «⋯» → «Editar» → marcar o desmarcar *Alfarería* → «Guardar cambios», esperando la respuesta y comprobando la línea en la fila visible («Owner restricts an assistant to one line»); la redirección del ayudante desde `/settings/*` sigue cubriendo «Ayudante intenta abrir la pantalla».
- [x] 7.6 `tests/e2e/account.spec.ts:137`: buscar el nombre dentro de las filas visibles de `member-row` (tabla y tarjetas conviven en el DOM).
- [x] 7.7 `tests/e2e/accessibility.spec.ts`: además de `/settings/lines`, auditar con el diálogo «Crear línea» abierto y con la confirmación de archivar abierta, sin fallos críticos.

## 8. Cierre

- [x] 8.1 `npm run lint`, `npm run typecheck` y `npm run test:unit` en verde.
- [x] 8.2 `npx playwright test tests/e2e/settings.spec.ts tests/e2e/status-config.spec.ts tests/e2e/invitation.spec.ts tests/e2e/assistant-permissions.spec.ts tests/e2e/account.spec.ts tests/e2e/accessibility.spec.ts tests/e2e/platform-admin.spec.ts` en verde, y la suite completa antes de fusionar (≈ 18 min).
  - Estado (2026-09-14): los siete archivos, en verde en escritorio y móvil (125 pasadas; `platform-admin.spec.ts:112` falló una vez en paralelo y pasó 3 de 3 aislado). Suite completa contra el `next dev` compartido: 320 pasadas y 31 fallos repartidos casi todos entre suites ajenas a este cambio (inventario, pedidos, tareas, egresos, imágenes), el mismo patrón anotado en KAM-26; repetidos con `--last-failed --workers=2`, **pasan los 31**. Falta la corrida de CI contra el build antes de fusionar.
- [x] 8.3 Recorrido manual en el navegador con la dueña de Geeko Store por Líneas, Canales, Categorías, Unidades, Estados y Usuarios y roles (crear, editar, archivar, restaurar, invitar, editar un miembro, restaurar estados por defecto y cancelar cada confirmación), en escritorio y a 390 px; capturas de los diálogos.
- [x] 8.5 `actions/configuration.ts`: el identificador se valida con `z.guid()` y no con `z.uuid()`, que rechazaba los de la semilla y hacía fallar editar, archivar y restaurar lo sembrado con «Invalid UUID» (design, «Corrección al implementar»). `actions/configuration.test.ts` lo fija con identificadores con la forma de la semilla (fallaba antes del arreglo, pasa después); verificado en la Geeko de la semilla editando «Feria» y «Alfarería» y devolviéndolas a su nombre.
- [x] 8.4 `openspec validate settings-dialogs-and-tables --strict` sin errores y `graphify update .` tras el último cambio de código.

## 9. Menú lateral de secciones (pedido tras la primera implementación)

- [x] 9.1 `features/settings/settings-nav.tsx`: `SETTINGS_SECTIONS` con `group` (*Organización*, *Equipo*, *Preferencias*, *Datos*) e `icon`, Notificaciones antes que Retención; un solo `<nav>` que es columna `sticky` desde `lg` con títulos de grupo y fila con desplazamiento horizontal por debajo, sin títulos; grupos vacíos omitidos; el enlace actual con `aria-current="page"`, aspecto seleccionado y desplazado a la vista al montarse (design D10).
- [x] 9.2 `app/(app)/settings/layout.tsx`: bloque `max-w-screen-xl` alineado con el título (sin `mx-auto`) con rejilla `lg:grid-cols-[13rem_1fr]`, menú a la izquierda y contenido `min-w-0` a la derecha.
- [x] 9.3 `features/settings/settings-nav.test.tsx`: las pruebas existentes siguen pasando; se suman los grupos del dueño en orden con sus secciones y el enlace actual marcado («Owner sees the menu beside the content on a wide screen», nivel unitario), y que el ayudante solo ve *Preferencias* y *Datos*, sin títulos vacíos («The assistant sees only their groups»).
- [x] 9.4 `tests/e2e/settings.spec.ts`: a 1280 px el menú queda a la izquierda del contenido y marca la sección actual («Owner sees the menu beside the content on a wide screen»); a 1920 px el bloque no pasa de 1280 px y empieza en el mismo borde que el título («The settings block does not stretch past its maximum width»); a 390 px el menú es una fila, «Usuarios y roles» está a la vista y marcada, y la página no se desplaza a lo ancho («On a phone the menu is one row that scrolls sideways»).
- [x] 9.5 Recorrido con capturas a 1440, 1024, 768 y 390 px, en tema claro y oscuro; auditoría de accesibilidad de `/settings/lines` en verde; lint, typecheck y unitarias en verde; `openspec validate --strict` y `graphify update .`.
  - Estado (2026-09-14): capturas a 1440, 1024, 768 y 390 px en claro y oscuro revisadas; de paso, las descripciones de sección y el párrafo de *Reparto de gastos compartidos* se limitan a `max-w-prose` (a 1440 px pasaban de 110 caracteres por línea). Lint, typecheck y 2.325 unitarias en verde; `accessibility`, `settings`, `status-config`, `invitation`, `account` y `my-tasks` en escritorio y móvil: 70 de 71, y la que falló (`account.spec.ts:272`, cerrar sesión con registros pendientes, ajena a configuración) pasa 3 de 3 aislada.
