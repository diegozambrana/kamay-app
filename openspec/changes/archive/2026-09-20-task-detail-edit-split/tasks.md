## 1. Vista de origen de tareas

- [x] 1.1 `lib/tasks/list-href.ts` (design D5): `sanitizeFrom(from)` conserva solo `view`, `q`, `assignee`, `tag`, `status`, `archived`, `link`, `nodeliv`, `closed` y descarta el valor entero si trae `/` o `:`; el token reservado `my-tasks` identifica el origen *Mis pendientes*. `tasksListHref(from, extra?)` → `/tasks`, `/tasks?…` o `/my-tasks`. `originCrumb(from)` → `{ label: "Tareas" | "Mis pendientes", href }`. `withFrom(href, from)` para propagar.
- [x] 1.2 `lib/tasks/list-href.test.ts`: las nueve llaves se conservan y las demás se descartan («Volver al calendario filtrado»); sin `from` → `/tasks` en su vista por omisión («Enlace directo»); `from=my-tasks` → `/my-tasks` con el rótulo «Mis pendientes» («Desde Mis pendientes se vuelve a Mis pendientes»); URL absoluta, `//host`, ruta con `/` o `:` y llaves desconocidas → se descartan («Origen ajeno ignorado»).

## 2. Remapeo de estado al cambiar de línea

- [x] 2.1 `lib/tasks/line-change.ts` · `retargetStatusForLine(current, destinationStatuses)` → `moved | kept | impossible`, delegando en `targetStatusFor` de `lib/orders/kind-board.ts` (design D4). `services/tasks/task-service.ts` · `updateFields` ya aceptaba `businessLineId` y `remindAt` desde KAM-16: no hace falta tocarlo. La resolución del juego de la línea destino vive en la acción, como dice D4, porque es ella la que ya orquesta `StatusService` y `TaskService` para `moveTaskToStatus`. *(Desviación del plan, que la ubicaba en el servicio: se sigue el design.)*
- [x] 2.2 `lib/tasks/line-change.test.ts`: dos estados del mismo tipo → gana el de menor `position` («Varios estados del mismo tipo»); nombres distintos entre líneas no impiden la correspondencia («El nombre del estado no decide el destino»); sin estado de ese tipo → `impossible` («La línea nueva no tiene ese tipo»); el estado que ya pertenece al juego destino se conserva, para no ensuciar la bitácora con un cambio que no ocurrió; los archivados del juego destino se ignoran.

## 3. La acción de guardado

- [x] 3.1 `actions/tasks.ts` · `updateTaskFields` (design D3): ampliar `updateTaskSchema` con `businessLineId`, `remindAt` y `tagNames`; leer la tarea antes de escribir; rechazar si está archivada; rechazar un recordatorio sin fecha límite —comprobado contra el valor resultante, no contra el que la pantalla creía tener—; quitar la fecha límite limpia el recordatorio; con línea nueva, resolver el estado destino (2.1) y escribir línea y estado en la misma operación, o devolver error legible si no hay destino; `revalidatePath("/tasks/[id]")` además de `revalidateTasks()`. Los campos `undefined` no se escriben (design D2).
- [x] 3.2 `actions/tasks.ts` · `updateTaskField`: estrechar la unión discriminada al caso `statusId`, retirando `title`, `businessLineId`, `assigneeId`, `dueDate` y `remindAt`. No se toca su emisión de avisos (design D3, pendiente deliberado).
- [x] 3.3 `actions/tasks.test.ts`: `updateTaskFields` rechaza el título vacío con su mensaje («El título no puede quedar vacío»), el recordatorio sin fecha límite («Un recordatorio necesita fecha límite») y la tarea archivada; quitar la fecha límite manda también `remindAt: null` («Quitar la fecha límite quita el recordatorio»); una carga con tres campos escribe esos tres y ninguno más («Un campo que no se tocó no se registra»); sin destino de estado en la línea nueva no se escribe nada («La línea nueva no tiene ese tipo»).

## 4. El formulario, con modo edición

- [x] 4.1 `features/tasks/task-form.tsx` (design D6): migrar a react-hook-form con Zod, conservando campos, validación y destino del alta. Prop `mode: "create" | "edit"` con los datos de la tarea en edición; el recordatorio solo en edición; el bloque de vínculo prellenado solo en alta.
- [x] 4.2 `task-form.tsx` edición: `useDiscardConfirm(isDirty)` de `features/orders/discard-guard.tsx` para cancelar, para las migas y para abandonar la página; `reset()` antes de navegar tras guardar, para que la salida no pregunte.
- [x] 4.3 `task-form.tsx` edición: Guardar arma la carga **solo con los campos sucios** (`dirtyFields`, design D2), espera el resultado de `updateTaskFields` con el botón en «Guardando…» y navega a `/tasks/[id]` con el origen conservado; un error deja el formulario abierto con su mensaje y los datos escritos.
- [x] 4.4 `features/tasks/task-form.test.tsx`: las pruebas vigentes del alta conservan **todas** sus aserciones intactas («El alta no cambia»). *(Desviación del plan, que las quería sin tocar: `handleSubmit` de react-hook-form resuelve en una microtarea, así que «pulsar y comprobar en el mismo tic» ya no vale y cinco casos pasan a `async` + `waitFor`. Es una adaptación del arnés, no de lo que se verifica: los cinco fallaban con `Number of calls: 0`.)*; en edición el formulario llega con los valores actuales y sin estado ni cuerpo («El formulario llega con los valores actuales»); guardar navega al detalle («Guardar lleva al detalle con los valores nuevos»); título vacío y recordatorio sin fecha límite se impiden en pantalla («El título no puede quedar vacío», «Un recordatorio necesita fecha límite»); solo viajan los campos tocados («Un campo que no se tocó no se registra»); un fallo de la acción deja el formulario abierto («Un fallo deja el formulario abierto»); salir con cambios pregunta y sin cambios no («Salir con cambios pide confirmación», «Salir sin cambios no pregunta nada»); tras guardar la salida no pregunta («Salir tras guardar no pregunta nada»); migas `Tareas › <título> › Editar` con los dos primeros tramos enlazados («La edición de una tarea nombra su tarea»).

## 5. La ruta de edición

- [x] 5.1 `app/(app)/tasks/[id]/edit/page.tsx`: página delgada al estilo de `app/(app)/orders/[id]/edit/page.tsx` — sesión, `TaskService.getById` (RLS decide: `null` → `notFound()`), líneas activas, responsables y etiquetas de la organización; lee `searchParams.from`. Tarea archivada: no rinde el formulario, explica su estado y conserva las migas.
- [x] 5.2 `app/(app)/tasks/[id]/edit/{loading,error}.tsx`: siguiendo la forma de los de `orders/[id]/edit`, con el tramo padre en el esqueleto. Satisface `app/route-states.test.tsx`, que descubre los segmentos solo.
- [x] 5.3 `components/layout/mobile-nav.tsx` (design D9): añadir el patrón de `/tasks/[id]/edit` a las rutas sin barra inferior; `/^\/tasks\/[^/]+$/` no casa con ella. Caso en `components/layout/mobile-nav.test.tsx` («En móvil ocupa la pantalla completa»).

## 6. El detalle, sin controles de cabecera

- [x] 6.1 `features/tasks/detail/task-fields.tsx`: título, línea, responsable, fecha límite, recordatorio y etiquetas pasan a texto; un dato sin valor se dice, no se deja en blanco. El estado se conserva como acción con su etiqueta de color (design D8), manteniendo `opensClosingWizard` y el salto a `?close=`. Acción *Editar* hacia `/tasks/[id]/edit` con el origen, oculta en una tarea archivada.
- [x] 6.2 `app/(app)/tasks/[id]/page.tsx`: leer `searchParams.from` y pasarlo al detalle; pasar las etiquetas de la tarea (ya vienen en `Task.tags`). Conservar el arreglo que añade al juego un estado archivado (design D8).
- [x] 6.3 `features/tasks/detail/task-detail.tsx`: migas con el tramo de origen (`originCrumb`); `onClosed` del asistente de cierre vuelve al origen en lugar de a `/tasks` fijo.
- [x] 6.4 `features/tasks/detail/task-fields.test.tsx`: la cabecera se rinde como texto y sin controles («La cabecera se lee, no se edita»); las etiquetas se muestran («Las etiquetas se ven en el detalle»); sin responsable y sin fecha límite ambos se dicen ausentes («Un dato ausente se dice»); la acción *Editar* apunta a `/tasks/[id]/edit` («Editar lleva al formulario») y no se rinde con la tarea archivada («Una tarea archivada no se edita»); cambiar el estado sigue guardando ahí mismo («El estado se cambia sin salir del detalle») y llevarlo a `final` con entregables pendientes abre el asistente («Cerrar con entregables pendientes sigue abriendo el asistente»).
- [x] 6.5 `features/tasks/detail/task-detail.test.tsx` (o el archivo que cubra el detalle): el editor de Markdown sigue rindiéndose y guardando («El cuerpo se escribe en el detalle»), la casilla se marca sin salir del detalle («Marcar una casilla no exige editar»), y adjuntos, vínculos y entregables siguen presentes («Adjuntos, vínculos y entregables siguen en el detalle»).

## 7. Propagar el origen desde las cuatro entradas

- [x] 7.1 `features/tasks/board/board-view.tsx` (tarjeta y salto al asistente) y `list-view.tsx` llevan `from` con la consulta actual, que `tasks-screen.tsx` calcula una sola vez. *(Las filas de la lista no enlazaban al detalle: ahora sí, decisión tomada en marcha. `calendar-view.tsx` se queda sin enlace, igual que el calendario de pedidos; el escenario «Volver al calendario filtrado» se reescribió como «Volver a la lista filtrada».)*
- [x] 7.2 `features/tasks/my-tasks/pending-row.tsx`: el enlace al detalle lleva `from=my-tasks`.
- [x] 7.3 Pruebas de las pantallas tocadas: el enlace al detalle incluye el origen; desde *Mis pendientes* incluye el token reservado («El origen sobrevive a la edición», «Desde Mis pendientes se vuelve a Mis pendientes»).
- [x] 7.4 Regresión de las migas que este cambio no altera: `components/layout/main-container.test.tsx` («El último tramo no es un enlace»), `features/orders/order-detail.test.tsx` («Detalle vuelve a la lista»), `features/orders/order-form.test.tsx` («Edición vuelve al detalle o a la lista», «Alta vuelve a la lista»), `features/catalog/item-detail.test.tsx` («Un solo camino de vuelta»), las pruebas del panel de plataforma («Panel de plataforma») y las de las pantallas de lista («Las listas no llevan migas») siguen en verde sin tocarse: el requisito de migas solo gana una fila en su tabla.

## 8. Integración

- [x] 8.1 `tests/integration/task-edit.test.ts` (nuevo): una edición que cambia título, responsable y fecha límite deja **un** registro de bitácora con esos tres campos, con valor anterior y nuevo, y ninguno más («Tres campos, un solo registro de bitácora», «Un campo que no se tocó no se registra»). El caso se arma con campos de la fila `tasks`, nunca con etiquetas: `task_tags` no lleva auditoría (design D10).
- [x] 8.2 `tests/integration/task-edit.test.ts`: cambiar la línea reubica el estado al primero de su mismo tipo en la línea nueva y la tarea aparece en esa columna de su tablero («La tarea aparece en el tablero de su línea nueva»); la bitácora registra el cambio de línea y el de estado («Ambos cambios quedan en la bitácora»); una línea destino sin estado de ese tipo deja la tarea con su línea y su estado anteriores («La línea nueva no tiene ese tipo»).
- [x] 8.3 `tests/integration/task-edit.test.ts`: una tarea archivada no se edita por la acción, y un ayudante sin acceso a la línea de la tarea no la alcanza («El ayudante no edita lo que no ve»).

## 9. e2e

- [x] 9.1 `tests/e2e/task-edit.spec.ts` (nuevo): desde el tablero con un filtro, abrir una tarea, pulsar *Editar*, cambiar tres campos, guardar, aterrizar en el detalle con los valores nuevos y volver por la miga al tablero con su filtro («Guardar aterriza en el detalle sin perder el origen», «Guardar lleva al detalle con los valores nuevos»).
- [x] 9.2 `tests/e2e/task-edit.spec.ts`: salir de la edición con cambios sin guardar pide confirmación («Salir con cambios pide confirmación»); abrir `/tasks/[id]/edit` de una tarea archivada explica su estado sin ofrecer formulario («La edición de una tarea archivada lo explica»); marcar una casilla desde el detalle sin entrar a editar («Marcar una casilla no exige editar»).
- [x] 9.3 `tests/e2e/assistant-permissions.spec.ts`: un ayudante sin acceso a la línea abre `/tasks/[id]/edit` y no obtiene la tarea («El ayudante no edita lo que no ve»); reutilizar el caso vigente del detalle («Una tarea de otra organización no se abre»).
- [x] 9.4 El caso de 390 px vive en `tests/e2e/task-edit.spec.ts` y no en `accessibility.spec.ts`: la pantalla completa, sin barra inferior y sin desplazamiento horizontal («En móvil ocupa la pantalla completa»). *(Se agrupó con el resto de la edición, que es donde está su preparación.)*
- [x] 9.5 Revisar las e2e que hoy editan campos desde el detalle de tarea —búsqueda de `task-title`, `task-assignee`, `task-due`, `task-line`, `task-remind`— y pasarlas por la pantalla de edición. Los casos que abren el detalle desde el tablero o desde un vínculo siguen valiendo («Se llega desde el tablero», «La dirección es compartible»).

## 10. Documentos y cierre

- [x] 10.1 `specs/PRD/kamay-mapa-navegacion-ui.md`: V18 pasa a dos pantallas, con la transición al editor y la vuelta, y la nota de pantalla completa en móvil para ambas.
- [x] 10.2 `specs/PRD/kamay-backlog-sprint-01.md`: marcar KAM-29 como comprometido y anotar en la bitácora del documento las tres decisiones que resolvieron sus preguntas abiertas (cuerpo en el detalle, guardado directo, línea con remapeo por tipo).
- [x] 10.3 `lint` y `typecheck` limpios; `test:unit` 292 archivos / 2861 pruebas; `test:integration` 21 archivos / 125 pruebas; e2e de tareas en serie, 46 de 47 —el fallo suelto pasa aislado: es contención sobre la base local compartida—. Verificación visual hecha en escritorio y en 390 px, y destapó que las migas de tres tramos empujaban «Editar» fuera de la pantalla: `components/layout/main-container.tsx` recorta ahora los tramos intermedios, con sus dos pruebas.
