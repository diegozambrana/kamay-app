> Cada tarea de prueba nombra los escenarios del delta spec que cubre (convención nº 12: ningún escenario sin prueba referenciada). Los escenarios viven en `specs/*/spec.md` de este cambio. Las decisiones citadas (D1–D14) son las de `design.md`.
>
> **KAM-01 a KAM-22 están archivadas**: todo el alcance es ejecutable y ningún grupo está bloqueado. El orden sigue el plan de migración de `design.md`.

## 0. Condición de arranque

- [x] 0.1 Confirmar que KAM-17 a KAM-22 siguen archivadas y que no hay otro cambio vivo en `openspec/changes/` que reescriba los requisitos que este cambio toca (`project-foundation`, `tenant-isolation`, `activity-retention`).
- [x] 0.2 Confirmar el alcance de base de datos: **dos migraciones nuevas** —la política de `activity-exports` (grupo 7) y el hook de alta (grupo 8)—, cada una con su pgTAP, y ninguna alteración de tablas, vistas o índices existentes. Si la verificación del anexo §20 (grupo 1) descubre un incumplimiento, la corrección entra como migración nueva con su pgTAP y se anota como hallazgo (convención nº 6).
- [x] 0.3 Inventariar las vistas con datos y su forma (lista, tabla, tablero, calendario, cuadrícula, gráfico, detalle), para que el grupo 2 sepa cuántos esqueletos distintos hacen falta.
  - *Resultado:* cinco formas. **Tablero** (`/orders`, `/tasks`, con sus vistas de lista y calendario); **lista** (`/expenses`, `/contacts`, `/catalog`, `/activity`, `/assets`, `/my-tasks` y las secciones de `/settings`); **detalle** (`/orders/[id]`, `/expenses/[id]`, `/catalog/[id]`, `/tasks/[id]`); **formulario** (las altas y `/orders/[id]/edit`, que cargan catálogos de referencia); **tarjetas** (`/dashboard`, `/reports`, `/quick`). Todas las páginas son componentes de servidor que consultan antes de rendir, así que el estado de carga solo puede venir de `loading.tsx`.
  - *Hallazgo:* `MainContainer` ya declara `loading`, `isEmpty` y `error`, y `DataTable` declara `empty`, pero **ninguna vista usa los tres primeros**: el «Cargando…» es texto, el vacío no lleva acción y el error no ofrece reintentar. El juego del grupo 2 los sustituye en lugar de convivir con ellos: una sola implementación de cada estado.

## 1. Controles de catálogo — el anexo §20 primero (D13)

- [x] 1.1 Crear `supabase/tests/views_security.test.sql`: recorrer `pg_class` filtrando vistas de los esquemas de la aplicación y comprobar `security_invoker=true` en `reloptions`, **sin enumerar ninguna vista**. `throws_ok` de cuatro argumentos, ejecutado con `supabase test db`, nunca con `psql`. Cubre `tenant-isolation` → *Every view declares security_invoker* → «No view lacks the option», «A new view without the option fails the build».
- [x] 1.2 Añadir al mismo archivo la comprobación de comportamiento: dos organizaciones sembradas, un miembro de A consulta cada vista derivada y obtiene cero filas de B. Cubre → «A view does not widen what its caller may read».
- [x] 1.3 Crear `supabase/tests/preproduction_checklist.test.sql`: toda tabla de datos con `organization_id` y RLS activo. Cubre *The pre-production database checklist is verified point by point* → «Every table carries organization_id with RLS enabled».
- [x] 1.4 Añadir la cobertura de disparadores de auditoría sobre todas las tablas auditables, incluidas las de KAM-17 a KAM-22. Cubre → «Audit triggers cover every auditable table».
- [x] 1.5 Añadir la comprobación de tipos de importe. Cubre → «Amounts are never floating point».
- [x] 1.6 Añadir la comprobación de rutas de Storage alcanzadas por organización, apoyándose en lo que `attachments_storage.test.sql` ya siembra. Cubre → «Storage paths are scoped by organization».
- [x] 1.7 Comprobar que las pruebas existentes (`expense_access`, `asset_access`, `inventory_access`, `direct_sale_access`, `order_idempotency`) cubren los puntos del ayudante sin acceso a `expenses`, `asset_details` e `item_last_cost` y de la venta de feria reenviada; si alguno no está, escribirlo.
- [x] 1.8 Verificar a mano los puntos no automatizables —índices que cubren los filtros reales de la interfaz, ninguna columna con valor derivable— y escribir los doce puntos con su resultado y su prueba en `docs/anexo-bd-verificacion.md`. Cubre → «The checklist result is documented».
- [x] 1.9 Si algún control descubre un incumplimiento, corregirlo con migración nueva y su pgTAP, y regenerar el grafo (`graphify update .`).

## 2. Juego de componentes de estado (D1, D2)

- [x] 2.1 Crear `components/shared/empty-state.tsx` sobre `components/ui/empty.tsx`: mensaje breve y acción, sin ilustración ni texto motivacional.
- [x] 2.2 Crear `components/shared/filtered-empty-state.tsx`: texto propio distinto del vacío inicial y acción «Quitar filtros».
- [x] 2.3 Crear los esqueletos de `components/shared/skeletons/` sobre `components/ui/skeleton.tsx`, uno por forma inventariada en 0.3. Sin giradores.
- [x] 2.4 Crear `components/shared/error-state.tsx`: explicación en lenguaje humano, reintentar cableado al `retry()` de Next 16.3 —vuelve a pedir los datos; `reset()` no lo haría—, y ningún código, traza ni identificador interno visible.
- [x] 2.5 Crear `hooks/use-filter-state.ts` con `hasActiveFilters` y `clearFilters()` (D2).
- [x] 2.6 Pruebas unitarias del juego: `components/shared/*.test.tsx`. Cubre `view-states` → *Every data view presents an initial empty state* → «Empty state carries no decoration»; *Every data view presents a human error state with retry* → «Retry recovers without a full reload», «No technical detail reaches the user»; *The four states are consistent across the application* → «Two different views present the same state identically».

## 3. Aplicación de los estados a las vistas (D1, D3)

- [x] 3.1 Crear `app/global-error.tsx` y `app/not-found.tsx`.
- [x] 3.2 Añadir `loading.tsx` y `error.tsx` a pedidos (`/orders`, `/orders/[id]`) y sustituir sus mensajes sueltos por el juego compartido, distinguiendo vacío inicial de sin resultados. `order-*.spec.ts` en verde antes de pasar a la siguiente pantalla.
  - *Hecho:* `loading.tsx`, `error.tsx` y el juego compartido aplicados (grupo 2, prueba de cobertura 3.11). Verde con la suite completa contra la compilación de producción, dos vueltas seguidas sin reintentos (624 de 624), con cada prueba sobre su propia copia de la semilla.
- [x] 3.3 Lo mismo para egresos (`/expenses`, `/expenses/[id]`), con `expenses.spec.ts` en verde.
  - *Hecho:* ver 3.2.
- [x] 3.4 Lo mismo para catálogo (`/catalog`, `/catalog/[id]`, con su inventario) y contactos (`/contacts`).
  - *Hecho:* ver 3.2.
- [x] 3.5 Lo mismo para tareas (`/tasks`, `/tasks/[id]`, con vínculos y entregables) y *Mis pendientes* (`/my-tasks`), con `task-*.spec.ts` y `my-tasks.spec.ts` en verde.
  - *Hecho:* ver 3.2.
- [x] 3.6 Lo mismo para activos (`/assets`) e informes (`/reports`), con `assets.spec.ts` y `reports.spec.ts` en verde.
  - *Hecho:* ver 3.2.
- [x] 3.7 Lo mismo para la bitácora (`/activity`), con `activity.spec.ts` en verde, y para la bandeja de avisos.
  - *Hecho:* ver 3.2.
- [x] 3.8 Lo mismo para el panel (`/dashboard`) y la captura móvil (`/quick`).
  - *Hecho:* ver 3.2.
- [x] 3.9 Lo mismo para configuración (`/settings/*`), donde el vacío inicial de cada catálogo de configuración ofrece su acción de creación.
  - *Hecho:* ver 3.2.
- [x] 3.10 Comprobar que en el modo feria y en las vistas con datos ya cargados el indicador de sin conexión no sustituye el contenido ni se presenta como error. Cubre `view-states` → *The offline indicator is not one of these states* → «Losing connection does not blank the view».
- [x] 3.11 Crear la prueba de cobertura de rutas (D3): recorre `app/`, junta los segmentos con `page.tsx`, descuenta la lista de exclusiones —cada una con su motivo escrito— y falla si a alguno le falta `loading.tsx` o `error.tsx`.
- [x] 3.12 Pruebas e2e de los estados sobre dos vistas representativas: vacío inicial con su acción, filtrar hasta cero y quitar filtros, y fallo de carga con reintento. Cubre `view-states` → «Empty view offers its creation action»; *Filtered-empty is distinct…* → «Filtering to zero results shows the filtered-empty state», «Clearing filters restores the unfiltered view», «An empty organization never shows the filtered-empty state»; *Every data view presents a human error state with retry* → «Failed load explains itself and offers retry».
- [x] 3.13 Prueba de que ninguna vista usa girador como estado de carga de contenido. Cubre → *Every data view presents a loading state with skeletons* → «Loading shows the shape of the content», «No spinner replaces the content».

## 4. Cierre y estabilidad de la suite de extremo a extremo (D4)

- [x] 4.1 Separar en `task-deliverables.spec.ts` el cierre creando **todos** los entregables del cierre creando **algunos**, conservando el de cerrar sin crear ninguno. Cubre `project-foundation` → *The seven end-to-end journeys are covered and pass* → «Closing without deliverables is exercised».
  - *Hecho:* tres pruebas —todos, algunos y ninguno— en `task-deliverables.spec.ts`; verde contra la compilación de producción.
- [x] 4.2 Ampliar `assistant-permissions.spec.ts` a toda ruta reservada a la persona dueña —`/expenses` y sus altas, `/settings/retention`, las demás secciones reservadas de configuración, `/reports/export`, `/activity/export` y la nueva `/settings/export` para las secciones de costos—: ausente del menú y redirigida por dirección directa. Cubre → «The assistant journey covers every restricted route».
  - *Hecho:* bloque «toda ruta reservada a la dueña» con la lista `OWNER_ONLY` (menú en las dos superficies, dirección directa y descargas, incluida `/settings/export` para las secciones de costos); sus 62 pruebas en verde contra la compilación de producción.
- [x] 4.3 Sustituir los dos `waitForTimeout` que quedan por esperas ancladas a estado observable. Cubre *The end-to-end suite is stable, not merely green* → «No arbitrary timed waits remain».
  - *Hecho:* los dos `waitForTimeout` se sustituyeron por esperas ancladas (`expect.poll` en el correo de recuperación; en la independencia pedido-tarea, el archivado se prepara con la sesión de la dueña porque el detalle no ofrece archivar, y hasta ahora la prueba **no archivaba nada**). Una regla de ESLint (`no-restricted-syntax` en `tests/e2e/**`) impide que vuelvan.
- [x] 4.4 Comprobar que toda prueba crea su propia organización y no depende de datos de otra ni del orden. Cubre → «Tests do not interfere with each other».
  - *Hecho:* toda prueba e2e trabaja sobre su propia organización. Geeko Store, Kamay Histórico y Kamay Rendimiento se siembran con funciones `e2e.seed_*(p_org)` —mismos identificadores para la semilla, derivados para una copia— y `e2e.clone_*()` hace una copia en ~0,2 s, idéntica fila a fila (comprobado tabla por tabla). `tests/e2e/helpers/seed-copies.ts` las da con `geeko()`, `historico()` y `rendimiento()`; los seis bloques en serie comparten una copia por bloque (`geekoForBlock()`); `auth.spec` usa dueñas nuevas (`createFreshOrganization`, `addOrganizationFor`). Ninguna suite inicia sesión con un usuario de la semilla. El esquema `e2e` no está expuesto: se llama por `psql`. pgTAP sigue en 924/924.
- [x] 4.5 Añadir el trabajo de estabilidad a `.github/workflows/`: suite completa con `retries: 0` y `--repeat-each`, sobre `main` y bajo demanda. Conservar `retries: 2` en el paso normal (D4). Cubre → «Repeated runs give the same result»; y `project-foundation` → *Continuous integration enforces the pipeline* → «The stability job fails on an intermittent test».
  - *Hecho:* `.github/workflows/stability.yml` —`main` y `workflow_dispatch`, `--retries=0 --repeat-each` (3 por defecto) y después `@performance` ×3—; `ci.yml` conserva los reintentos de `playwright.config.ts`. Hoy fallará por las pruebas que mutan la semilla (4.4, 4.6): es el resultado que el trabajo debe dar.
- [x] 4.6 Corregir en su causa toda prueba que el trabajo de estabilidad revele como intermitente. Es trabajo previsto, no sorpresa.
  - *Avance (triaje de la primera vuelta completa, `e2e-full-1.log`):* de sus 12 fallos, cinco eran duplicados del *streaming* de `loading.tsx` —ya resueltos con la espera de `tests/e2e/helpers/test.ts` y la de `orderCode`—, uno la regresión de la ventana del catálogo (nota de 6.4), tres esperas de guardado de pedido bajo siete workers que pasan en toda vuelta posterior sin saturación, y tres —`task-detail` ×2 y `task-board`— fallan también en `main` sin este cambio. La independencia pedido-tarea, ya resuelta aquí, sigue fallando de vez en cuando más adelante en la espera de la lista de tareas, y eso también ocurre en `main`. `archive-restore` en móvil tenía la misma carrera que el contacto: navegaba con el desarchivado en vuelo; ahora espera la respuesta.
  - *Hecho:* causas corregidas —la carrera de desarchivar y de archivar antes de navegar (catálogo, contacto, vínculo de tarea), una prueba de móvil que solo pasaba por las tareas que otras dejaban en Geeko, la degradación de feria medida venta contra venta (ahora medianas de cinco; fallaba también en `main`), y el proyecto `deployment` como dependencia, que dejaba a `--repeat-each` repitiendo solo esa prueba—. Resultado local: 314 de 314 en una vuelta y 624 de 624 en dos vueltas seguidas, `--retries=0`, contra la compilación de producción. Con dos o más workers el contenedor local de Auth se queda sin puertos (cada vista valida la sesión 27–45 veces; ver *Open Questions* de `design.md`): no es un fallo de las pruebas, y el trabajo de CI confirmará la estabilidad en Linux.
- [x] 4.7 Dejar constancia de que `order-flow.spec.ts` es el `order-lifecycle` de ARCHITECTURE.md y comprobar que los siete recorridos pasan. Cubre → *The seven end-to-end journeys are covered and pass* → «All seven journeys run and pass».
  - *Hecho:* `order-flow.spec.ts` declara ser el `order-lifecycle`, y los siete recorridos —auth, order-flow, fair-offline, task-deliverables, assistant-permissions, status-config, archive-restore— pasan en las dos vueltas.
- [x] 4.8 Comprobar que el catálogo pgTAP de ARCHITECTURE.md está completo tras el grupo 1. Cubre → *The pgTAP catalog is complete* → «Every declared pgTAP test exists».
  - *Hecho:* con `views_security.test.sql` del grupo 1, las ocho pruebas pgTAP que enumera ARCHITECTURE.md existen.

## 5. Accesibilidad (D11)

- [x] 5.1 Añadir el motor de auditoría de accesibilidad integrado en Playwright y cablearlo a los estados que los recorridos ya alcanzan: tablero con datos, formulario con errores, diálogo abierto, modo feria, captura móvil e informes. Falla ante gravedad crítica o seria; el resto se registra.
  - *Hecho:* `@axe-core/playwright` en `tests/e2e/accessibility.spec.ts`: trece vistas principales más tablero con datos, formulario con errores, diálogo abierto y modo feria, en escritorio y móvil.
- [x] 5.2 Ejecutar la auditoría en tema claro y en tema oscuro. Cubre `accessibility` → *Text and interactive elements meet contrast thresholds* → «Contrast holds in both themes».
- [x] 5.3 Corregir los hallazgos críticos o serios de contraste, foco, etiquetas y nombres accesibles. Cubre → *Focus is always visible* → «Tabbing reveals focus on every control»; *Every control has an accessible name* → «Icon-only controls are named», «Form fields are labelled».
  - *Hallazgos corregidos:* dos `<main>` anidados en toda página (`SidebarInset` pasa a `div`); `/reports` sin región principal ni `<h1>`; la etiqueta de línea de la bitácora pintaba el token del color como color CSS (contraste 1,97); `--muted-foreground` del tema claro a 0.51 (4,34 → ~5,5:1); texto de la insignia destructiva a `red-700`; tablas desplazables sin acceso por teclado (`scrollLabel` en `Table`, doble contenedor de desplazamiento retirado en informes); el foco de los diálogos, paneles y alertas abiertos sin `DialogTrigger` caía en el `body` al cerrarlos (`useReturnFocus` en los tres primitivos); el botón de cerrar se anunciaba en inglés.
- [x] 5.4 Implementar la alternativa de teclado al arrastre en `components/board/` —tablero de pedidos y de tareas—, con el mismo efecto sobre el estado del registro que el arrastre, y anunciarla en el nombre accesible de la tarjeta. Cubre → *Board cards can be moved without a pointer* → «Keyboard moves a card between columns», «The keyboard alternative is announced».
  - *Hecho:* menú «Mover a…» por tarjeta en `components/board/kanban-board.tsx`, que llama al mismo `onMove` que el arrastre. De paso se retiraron los `attributes` de `useSortable`: daban `role="button"` y `tabindex` a un contenedor que envolvía el enlace de la tarjeta (control anidado) sin sensor de teclado detrás; las tarjetas de tarea no se podían abrir con el teclado y ahora sí (`role="link"`, Enter). Pruebas en `components/board/kanban-board.test.tsx`.
- [x] 5.5 Prueba e2e solo con teclado: abrir un diálogo, comprobar el atrapado de foco, cerrarlo con escape y ver el foco de vuelta; y completar un alta sin puntero. Cubre → *Every action is reachable by keyboard* → «A dialog traps and returns focus», «A record can be created without a pointer».
- [x] 5.6 Añadir la auditoría al paso de e2e del pipeline de pull request y comprobar que una violación crítica lo rompe y que la superficie cubre escritorio y móvil. Cubre → *The accessibility audit runs automatically and reports no critical failures* → «The audited surface covers the main views», «A critical violation breaks the build»; y `project-foundation` → *Continuous integration enforces the pipeline* → «CI fails on a critical accessibility violation», «CI passes on a healthy pull request», «CI fails on a broken typecheck».
  - *Hecho:* la auditoría forma parte de `npm run test:e2e`, que ya es el paso de e2e de `ci.yml`; en las primeras vueltas falló nombrando vista, regla y elemento, que es la evidencia de «A critical violation breaks the build».

## 6. Rendimiento (D12)

- [x] 6.1 Crear la semilla de rendimiento: organización propia con doce meses de pedidos, egresos, inventario, cobros, tareas, avisos y bitácora, generada de forma determinista. Cubre `performance-budget` → *The performance seed contains twelve months of data* → «The seed produces a year of activity in its own organization».
  - *Hecho:* `supabase/seeds/performance.sql` («Kamay Rendimiento», `rendimiento@kamay.test`): 624 pedidos, 1.248 líneas, 801 cobros y pagos, 208 egresos, 312 movimientos de inventario, 156 tareas, 60 avisos y ~3.800 eventos de bitácora en 13 meses calendario, generado sin `random()`. Registrado en `config.toml` y verificado por `supabase/tests/performance_seed.test.sql`.
- [x] 6.2 Crear la medición del panel principal con limitación de CPU y red al perfil de gama media, con umbral de 2 segundos hasta utilizable. Cubre → *The dashboard loads within its budget on a mid-range phone* → «Dashboard meets the budget with a year of data», «A regression fails the measurement».
  - *Hecho:* `tests/e2e/performance.spec.ts`, en el proyecto móvil, CPU ×4 y 4G lenta. Visita de vuelta —recursos estáticos en caché—: ~350 ms hasta compuesto y hasta `load`. Primera visita en frío, medida aparte y fuera del presupuesto: ~2,4 s (1,1 MB de JavaScript). Dos trampas corregidas en la propia medición: el service worker pide las navegaciones por su cuenta y la limitación de CDP no lo alcanza (se bloquea en esta prueba), y sin `Network.enable` Chrome acepta la emulación y no la aplica.
- [x] 6.3 Añadir la medición al trabajo de estabilidad, no al pull request (D12). Cubre `project-foundation` → *Continuous integration enforces the pipeline* → «The stability job fails when the dashboard exceeds its budget».
  - *Hecho:* etiqueta `@performance`; `ci.yml` la excluye del paso de e2e del PR y `stability.yml` la corre tres veces sin reintentos.
- [x] 6.4 Auditar toda vista de lista y aplicar paginación o ventana acotada donde falte, con límite explícito por petición y forma de traer el resto sin recargar. Cubre → *No data view loads an entire table* → «A list requests a bounded page».
  - *Hecho:* `lib/pagination.ts` (vueltas de 50, techo 1000, `takeWindow`, `chunk`) y `components/shared/load-more.tsx`. Pedidos y tareas: **todo lo abierto y una ventana de lo cerrado** (`listWindow`, `listBoardWindow`, `?closed=`), porque una ventana simple sacaría del tablero trabajo antiguo aún abierto. Contactos y catálogo: ventana por orden alfabético (`?limit=`), con el contacto abierto por enlace traído aparte si queda fuera. Egresos: la ventana es el periodo, que por omisión es el mes y sobre el que se suma el resumen; recortar filas falsearía el resumen. **Hallazgo:** con un año de pedidos, `totalsFor` respondía `414 URI too long` y el tablero caía en error; toda consulta por lista de identificadores va ahora en tandas de 100 (`order_totals`, líneas de pedido, adjuntos, insignias de tareas —que además se tragaban el error—, totales de egresos). El panel y pedidos piden los nombres de contacto de lo que muestran (`namesFor`), no el directorio. Pendiente anotado: los selectores de proveedor de los formularios siguen cargando el directorio completo, acotado por su tamaño. Prueba: `tests/e2e/pagination.spec.ts`.
  - *Regresión corregida:* la ventana alfabética escondía lo recién creado. Crear «Zapatos» en un catálogo de más de 50 ítems lo guardaba y no lo mostraba —y en el directorio, el contacto nuevo quedaba sin fila y con el detalle vacío—. El catálogo pasa `?created=` y la página lo trae aparte si los filtros vigentes lo habrían mostrado (`lib/catalog/window.ts`, con pruebas); el directorio pasa `?id=`, que ya traía aparte al contacto abierto por enlace. Dos pruebas nuevas en `pagination.spec.ts`, sobre una organización propia con 55 registros, fallan sin la corrección. Lo destapó `offline-capture` en la primera vuelta completa, cuando Geeko ya acumulaba más de 50 insumos de pruebas anteriores.
- [x] 6.5 Comprobar que la consulta de `/activity` se resuelve por página y filtros y nunca pide todos los eventos. Cubre → «The activity log is never loaded whole».
  - *Hecho:* la bitácora ya paginaba por cursor desde KAM-22; `pagination.spec.ts` lo verifica sobre ~3.800 eventos.
- [x] 6.6 Optimizar imágenes de catálogo, activos y miniaturas de adjuntos, con reserva de espacio en la disposición. Cubre → *Images are served optimized* → «A thumbnail is not the full-size image», «Layout does not shift on image load».
  - *Hecho:* miniatura WebP de 480 px generada al subir, en el servidor, con sharp (`lib/attachments/thumbnail.ts`), junto al original (`….thumb.webp`) y en la misma carpeta de la organización. `AttachmentService` la crea al subir —sin poder fallar la subida—, la copia con `copyToEntity` y la retira con `removeObjects`; `signedThumbnailUrls()` firma miniatura y original en la misma petición y cae al original si no hay miniatura. Catálogo, detalle de ítem, pedido, edición de pedido y comprobantes pintan la miniatura; el panel de adjuntos de la tarea la pinta y abre el original. Toda imagen ya tenía su caja fija. `tests/e2e/images.spec.ts`: una foto de ~2,4 MB se sirve como WebP de menos de una décima parte, a no más de 480 px, y la vista no se desplaza al cargarla (CLS < 0,01).

## 7. Exportación completa (D5, D6)

- [x] 7.1 Mudar `lib/reports/csv.ts` a `lib/export/csv.ts` con sus pruebas y actualizar las importaciones de `app/(app)/reports/export/route.ts` y `app/(app)/activity/export/route.ts`. Las pruebas de ambas rutas en verde en el mismo commit.
  - *Hecho:* `git mv` a `lib/export/csv.ts`, con `toTableCsv()` —la tabla sola, encabezados primero— y `csvLine`/`CSV_BOM` para escribir por partes.
- [x] 7.2 Añadir a `lib/export/` la composición del archivo comprimido como función pura, con la dependencia de compresión.
  - *Hecho:* `lib/export/archive.ts` con `fflate`: `zipStream()` por flujo, entradas enteras o por partes.
- [x] 7.3 Crear la migración de la política `select` sobre `storage.objects` para el bucket `activity-exports`, limitada a `is_owner()` de la organización de la primera carpeta, sin políticas de escritura ni borrado (D6).
  - *Hecho:* `20260911100000_activity_exports_owner_read.sql`.
- [x] 7.4 pgTAP de esa política. Cubre `data-export` → *The export includes the activity detail already purged by retention* → «Another organization's purge exports are unreachable», «An assistant cannot read purge exports».
  - *Hecho:* `supabase/tests/activity_exports_access.test.sql`.
- [x] 7.5 Crear `services/export/export-service.ts`: lectura de cada tabla por páginas hasta agotarla y de las exportaciones de la purga de la organización, con el cliente de sesión, sin service role (convención nº 1 y nº 2).
  - *Hecho:* `services/export/export-service.ts`, sobre el manifiesto `lib/export/tables.ts` (28 tablas y sus columnas, `ownerOnly` según RLS; `invitations.token_hash` excluido). Paginación por clave donde hay `id`. `tests/integration/export-manifest.test.ts` compara el manifiesto con el catálogo: una tabla o columna nueva sin exportar rompe CI.
- [x] 7.6 Crear `app/(app)/settings/export/route.ts` calcado de las rutas de exportación existentes, con escritura por flujo y `Content-Disposition`.
  - *Hecho con un cambio de ruta:* un segmento no admite `page.tsx` y `route.ts` a la vez, así que la pantalla vive en `/settings/export` y la descarga en `/settings/export/download` (`getSessionContext()`, no el de dueño: los dos roles exportan).
- [x] 7.7 Añadir el punto de entrada en la configuración de la organización, con indicación de progreso y explicación humana con reintento si falla. Cubre → *The export is available at any moment and reports its progress* → «Export is requested on demand», «A failed export explains itself».
  - *Hecho:* sección «Exportar» de V15 para los dos roles (`features/settings/export-section.tsx`), con el progreso en bytes y el fallo explicado con reintento. Pruebas en `export-section.test.tsx`; `settings-nav.test.tsx` ahora conoce las dos secciones personales.
- [x] 7.8 Registrar la exportación en la bitácora, sin que el fallo del registro impida ni deshaga la exportación. Cubre → *Exporting is recorded in the activity log* → «A completed export leaves a trace».
  - *Hecho con una tercera migración no prevista:* `activity_log.action` no admitía ninguna acción para una exportación y la tabla es inmutable para `authenticated`. `20260911110000_activity_exported.sql` añade `exported` y `record_export()` (`security definer`, solo miembros, siempre a nombre de quien llama). El evento se escribe al terminar el archivo y su fallo se reporta sin deshacerlo. La bitácora lo lee como «X exportó todos los datos de la organización». Prueba: `supabase/tests/export_log.test.sql`.
- [x] 7.9 Pruebas unitarias de `lib/export/`. Cubre → *Exported values preserve their meaning* → «Accents survive opening the file», «Amounts survive the round trip», «A relation can be reconstructed from the file»; y *A full export produces every table the requester can read* → «An empty table still appears».
- [x] 7.10 Pruebas de integración de la exportación con dos organizaciones sembradas, un ayudante, una tabla más larga que una página y una bitácora ya purgada. Cubre → «Every table is present in the export», «A large table is exported whole»; *The export never exceeds what the requester may read* → «Cross-organization data never leaks into an export», «An assistant exports without cost information»; y → «Purged detail travels with the export».
  - *Hecho:* `tests/integration/export.test.ts` —dueña, ayudante, otra organización, una bitácora de ~3.800 filas y una purga subida con la clave de servicio, como la escribiría la retención—.

## 8. Alta según invitación (D8)

- [x] 8.1 Crear la migración con la función del hook *before user created*: rechaza el alta si el correo no tiene una invitación vigente —no aceptada, no vencida, no archivada—, con `execute` revocado a `authenticated` y `anon`.
  - *Hecho:* `20260911120000_signup_requires_invitation.sql` —`hook_before_user_created(jsonb)`, `stable security definer`, `search_path` vacío; compara el correo sin mayúsculas ni espacios; `execute` revocado a `public`, `anon` y `authenticated` y concedido solo a `supabase_auth_admin`—. El rechazo responde 403 con un mensaje humano.
- [x] 8.2 Registrar el hook en `supabase/config.toml` y habilitar el alta del proveedor, dejando escrito en el propio archivo que la puerta la guarda la función.
  - *Hecho:* `[auth] enable_signup = true` y `[auth.hook.before_user_created]` en `supabase/config.toml`, con el porqué escrito junto a cada uno. `auth.admin.createUser()` no pasa por el hook: las semillas y las pruebas siguen creando cuentas con la clave de servicio.
- [x] 8.3 pgTAP de la función con los casos sin invitación, vigente, aceptada, vencida y archivada.
  - *Hecho:* `supabase/tests/signup_hook.test.sql` (9 pruebas: los cinco casos, mayúsculas y espacios, y los privilegios de `execute`).
- [x] 8.4 Prueba de integración contra la API de Auth local. Cubre `production-operations` → *Only a pending invitation can create an account* → «Signing up without an invitation is rejected», «A spent invitation does not open the door».
  - *Hecho:* `tests/integration/signup-invitation.test.ts` contra la API de Auth local: sin invitación (403, sin usuario), vencida, vigente —alta y canje por el mismo camino que `signUpAndAccept`— y una cuenta existente. La invitación se siembra con la sesión de la dueña, bajo RLS: el service role no tiene `insert` sobre `invitations`.
- [x] 8.5 Comprobar que el recorrido de invitación y `auth.spec.ts` siguen en verde. Cubre → «A pending invitation lets the invitee sign up», «Existing users are unaffected».
  - *Hecho con una prueba nueva:* no había e2e del recorrido de invitación. `tests/e2e/invitation.spec.ts` lo cubre de punta a punta —la dueña invita desde V15, otra persona abre el enlace sin sesión, crea su cuenta y entra a la organización; un enlace sin invitación no crea cuenta—. `invitation.spec.ts` y `auth.spec.ts` en verde. Antes de este grupo el alta del invitado fallaba siempre (`signup_disabled`): era un hallazgo, no una regresión.

## 9. Trabajos programados (D7)

- [x] 9.1 Hacer que `app/api/notifications/daily/route.ts` responda a `GET`, el método con el que llama el programador de Vercel, conservando `isAuthorizedCron()`.
  - *Hecho:* el resumen diario exporta `GET` en lugar de `POST`; nada más lo llamaba con `POST`.
- [x] 9.2 Crear `app/api/activity/retention/route.ts`: comprueba el secreto, recorre las organizaciones con el cliente de service role y llama a `RetentionService.run()` una por una, cada una en su propio `try`, reportando cada fallo al monitoreo.
  - *Hecho:* la ruta comprueba el secreto antes de construir el cliente y delega en `services/activity/retention-job.ts` (`runRetentionJob()`, con sus dependencias inyectables y prueba unitaria). Organizaciones vivas por antigüedad, un `try` por organización, `reportError()` con el trabajo y la organización. Responde 500 si alguna falló, para que el fallo se vea también en el registro del programador.
  - *Corrección posterior:* los dos trabajos leían las organizaciones en una sola consulta, y PostgREST corta en mil filas: pasada la milésima, las demás se quedaban sin retención ni resumen, sin error. Lo destapó la prueba de integración con las ~2.000 organizaciones que dejan las copias de la suite e2e. `OrganizationService.listActiveForJobs()` las lee por páginas de 500 hasta agotarlas, con prueba unitaria.
- [x] 9.3 Añadir la entrada mensual de la retención a `vercel.json`, junto a la del resumen diario.
  - *Hecho:* `0 4 1 * *` —el día 1 a las 04:00 UTC—.
  - *Corrección posterior (tarea 10.4):* la entrada horaria del resumen diario salió de `vercel.json` y pasó a `pg_cron`, porque el plan Hobby rechaza cualquier cron más frecuente que diario. `vercel.json` queda solo con la retención.
- [x] 9.4 Pruebas de integración de los dos puntos de entrada llamados como lo haría el programador. Cubre `production-operations` → *Scheduled jobs run in production and only for whoever holds their secret* → «A call without the secret is rejected», «A missing secret closes the door», «One organization's failure does not stop the others».
  - *Hecho:* `tests/integration/scheduled-jobs.test.ts` llama a los dos `GET` con y sin `Bearer`: sin credencial, con otra, sin esquema y sin `CRON_SECRET` configurado, 401 sin construir el cliente de service role. El fallo aislado se prueba con dos organizaciones propias con detalle vencido: la primera con Storage roto falla, se reporta y conserva su detalle; la segunda, posterior, se exporta y vacía de verdad; el resto de la base local se atiende con una rutina vacía para no vaciar las semillas. Una prueba más comprueba que cada entrada de `vercel.json` llega a un `GET`.
- [x] 9.5 Actualizar `supabase/README.md` § Trabajos programados: la retención ya está programada, por qué con el Cron de Vercel y no con `pg_cron`, y cómo vigilar su resultado.
  - *Hecho:* § *Agendada con el Cron de Vercel (KAM-23)* sustituye a *No está agendada, y es a propósito*. El requisito de `activity-retention` que decía «nunca por sí sola» cambia en `specs/activity-retention/spec.md` (renombrado y modificado).
- [ ] 9.6 Tras desplegar, comprobar en los registros de producción la primera ejecución de cada trabajo. Cubre → «Retention runs on its schedule».

## 10. Entorno, secretos y despliegue (D14)

- [ ] 10.1 Separar las variables de entorno por ambiente y actualizar `.env.example` con toda variable requerida y ningún valor real.
  - *Hecho en el repositorio, pendiente en Vercel:* `.env.example` enumera toda variable, dónde es requerida y la regla de un juego por ambiente; CI genera su propio `CRON_SECRET` enmascarado por ejecución. Falta cargar los juegos «Preview» y «Production» en el proyecto de Vercel, que aún no está vinculado al repositorio.
- [x] 10.2 Validar las variables requeridas al arrancar con un esquema —`CRON_SECRET` y `APP_URL` incluidas—. Cubre `production-operations` → *Environments are separated and secrets never reach the browser* → «A missing variable fails at startup, not at first use».
  - *Hecho:* `lib/env.ts` (zod) desde `instrumentation.ts` → `register()`. Siempre: las tres de Supabase; en producción además `CRON_SECRET` (≥ 16 caracteres) y `APP_URL`. Rechaza también una variable `NEXT_PUBLIC_*` que contenga la clave de service role. El error nombra la variable y nunca su valor. Comprobado con `next start`: sin las dos variables el servidor no atiende y el registro dice cuáles faltan; con ellas, arranca. `register()` no corre en `next build`.
  - *Corrección tras el primer despliegue:* en Vercel la validación dentro de `register()` tumbó la aplicación entera —500 en toda petición, `/sw.js` incluido—: `register()` corre también en el proxy, que no recibe las variables de servidor. La validación pasó a `next.config.ts`, en la fase de compilación y en `next start` fuera de Vercel: un entorno incompleto hace fallar el **despliegue** nombrando lo que falta, y la versión anterior sigue sirviendo. Es un momento más temprano que el arranque, que es lo que pide el escenario. Prueba en `lib/env-phase.test.ts`.
- [x] 10.3 Añadir la comprobación automática de que la clave de service role no aparece en la compilación servida al navegador. Cubre → «The service role key is absent from the client bundle».
  - *Hecho:* `npm run check:bundle` (`scripts/check-client-bundle.mjs`) revisa `.next/static`, `public/` y el HTML/RSC prerenderizado buscando el valor configurado, cualquier `sb_secret_…` y cualquier JWT con rol `service_role`; paso nuevo de `ci.yml` tras el build. Control negativo: con la clave pegada a un chunk, falla nombrando el archivo y no el valor.
- [x] 10.4 Confirmar que el plan de Vercel admite la frecuencia horaria del resumen diario, o ajustarla (riesgo de `design.md`).
  - *No la admite:* el plan Hobby limita los crons a uno diario y rechaza el despliegue entero —también las vistas previas— con «Hobby accounts are limited to daily cron jobs». Bajar la frecuencia degradaría el reparto por hora local (KAM-17), así que la pasada horaria pasó a `pg_cron`: `supabase/migrations/20260913113818_daily_notifications_pg_cron.sql` habilita `pg_cron` y `pg_net`, revoca `cron` y `net` a `anon`/`authenticated`/`public`, y agenda `kamay-daily-notifications` (`0 * * * *`) llamando a `GET /api/notifications/daily` con el `Bearer` que lee del Vault (`kamay_cron_secret`). Sin ese secreto el trabajo no llama a nadie, así que en local es inerte. `vercel.json` conserva solo la retención mensual, que en Hobby cabe de sobra. La prueba de `scheduled-jobs.test.ts` que exigía que las rutas programadas coincidieran con `vercel.json` ahora acepta las dos fuentes. Pendiente al desplegar: cargar el secreto en el Vault del proyecto alojado.
  - *Corrección posterior:* la primera versión escribía la dirección a mano, `kamay-app.vercel.app`, que sirve otra aplicación: al cargar el secreto, el `Bearer` de producción habría viajado a un tercero cada hora. `20260913143843_daily_notifications_url_from_vault.sql` reagenda el trabajo leyendo también la dirección del Vault (`kamay_app_url`) —solo llama si están las dos—, con 60 s de espera en lugar de los 5 s de `pg_net`. Su pgTAP, `scheduled_notifications_cron.test.sql`, comprueba el horario, que no quede ninguna dirección escrita, que sin dirección o sin credencial no se encole nada, la petición exacta con las dos, y que la credencial del Vault no la alcancen `anon` ni `authenticated`. El `revoke` sobre `net` de la migración anterior no tuvo efecto —el permiso lo concede `supabase_admin`—, y no expone nada: `net` no está en la Data API. Por decisión del usuario, el trabajo se activa después del primer despliegue, cuando se conozca la dirección real.
- [ ] 10.5 Desplegar en el dominio propio sobre conexión cifrada, con redirección del tráfico no cifrado y sin cabeceras de desarrollo. Cubre → *The application is deployed on its own domain with production settings* → «The production domain serves the application over HTTPS».
  - *Parte del repositorio hecha:* `poweredByHeader: false` —sin `X-Powered-By`—, comprobado en `production-settings.spec.ts`. El dominio, HTTPS y la redirección se hacen al desplegar.
- [ ] 10.6 Registrar el hook de alta en el proyecto alojado y comprobar en producción un alta sin invitación rechazada y una con invitación aceptada.
- [x] 10.7 Comprobar la invalidación del service worker en cada despliegue. Cubre → «A new deployment reaches returning users».
  - *Hecho:* registro con `updateViaCache: "none"` y comprobación de versión al volver a la pestaña (`ServiceWorkerProvider`), además del `no-store` de `/sw.js` y `skipWaiting`/`clientsClaim`. `production-settings.spec.ts` comprueba que `/sw.js` lleva el `BUILD_ID` —cada despliegue sirve otros bytes— y no se guarda; `deployment.spec.ts` simula el despliegue cambiando `public/sw.js` en disco y comprueba que la versión nueva toma el control de una pestaña abierta sin navegar. Corre en un proyecto de Playwright propio (`deployment`), después de desktop y mobile, para no cambiarle el service worker a otra prueba. Verde contra la compilación de producción.
- [x] 10.8 Declarar la aplicación como no indexable. Cubre → «The application is not indexable».
  - *Hecho:* `X-Robots-Tag: noindex, nofollow` en toda respuesta (`next.config.ts`) y `robots` en los metadatos raíz. Sin `robots.txt` con `Disallow` a propósito: impediría al rastreador leer el `noindex`. Prueba en `production-settings.spec.ts`.

## 11. Monitoreo de errores

- [ ] 11.1 Instalar el monitoreo en servidor y navegador, reportando versión desplegada, ruta y organización. Cubre `production-operations` → *Errors are monitored…* → «An unhandled error is reported with context».
  - *Hecho salvo el proveedor:* `onRequestError` de `instrumentation.ts` reporta todo error no controlado del servidor con la ruta como plantilla, el tipo de ruta y la organización de la cookie; los límites de error del navegador, con la ruta y la organización activa (`OrganizationProvider` → `setBrowserMonitoringScope`); la versión es `VERCEL_GIT_COMMIT_SHA`, inyectada como `KAMAY_RELEASE` en `next.config.ts`. Falta elegir el proveedor y enchufar su adaptador con `setMonitoringTransport()`; hasta entonces el reporte depurado queda solo en el registro de la función.
- [x] 11.2 Implementar la depuración explícita antes de enviar: sin nombres, contactos, importes, contenido de adjuntos ni credenciales.
  - *Hecho:* `lib/monitoring/scrub.ts`. Contexto de lista cerrada —versión, ruta, organización, límite o trabajo, `digest`, tipo de ruta— y limpieza por forma del mensaje y la traza: correos, teléfonos, importes, texto citado, la clave y la fila de los errores de Postgres, credenciales y consultas de una dirección. La ruta pierde su consulta y el token de una invitación. Todo pasa por `reportError()`, que es el único camino.
- [x] 11.3 Prueba que provoca un error sobre un registro con datos de cliente e importes y verifica que el reporte no los lleva. Cubre → «Personal data never reaches the monitoring service».
  - *Hecho:* `lib/monitoring/report-error.test.ts` provoca un error de cobro con nombre, correo, teléfono e importe de un cliente —con la forma en que PostgREST los devuelve— y comprueba que ni el reporte ni el registro local los llevan; `scrub.test.ts` e `instrumentation.test.ts` cubren cada forma y el camino del servidor.
- [x] 11.4 Comprobar que la indisponibilidad del monitoreo no añade fallos a la aplicación. Cubre → «Monitoring failure is invisible to the user».
  - *Hecho:* `reportError()` no lanza ni espera: pruebas con un proveedor que lanza, que rechaza, que no responde nunca y con la consola rota.

## 12. Copias de seguridad y restauración probada (D9, D10)

- [ ] 12.1 Elegir el destino de copias fuera del proveedor, con credenciales de solo escritura que no puedan leer ni borrar el histórico.
- [ ] 12.2 Crear el flujo de trabajo programado que vuelca esquema y datos por separado y sincroniza los objetos de Storage hacia ese destino, con cifrado en reposo.
- [ ] 12.3 Documentar periodicidad y retención. Cubre `production-operations` → *Backups run automatically…* → «A backup exists outside the provider», «Backups keep running without manual action».
- [x] 12.4 Escribir `docs/recuperacion.md` con el procedimiento de restauración —esquema sin disparadores, datos, disparadores— y la sección de bitácora de ensayos.
  - *Hecho y ejecutado como procedimiento:* `docs/recuperacion.md` —qué contiene una copia y con qué comando se produce cada archivo, restauración en una sola transacción con `session_replication_role = replica` (los disparadores no se ejecutan durante la carga y vuelven a funcionar al terminar: la forma que D10 admite para cargar sin disparadores), historial de migraciones, archivos, configuración fuera de la base, comprobación por conteos y recorrido, y la bitácora de ensayos—. Se ejecutó sobre un segundo proyecto local de Supabase recién creado: conteos idénticos en 31 tablas, mismas políticas, disparadores y funciones, inicio de sesión y RLS intactos. Dos hallazgos que el documento ya recoge: el volcado de esquema no trae las políticas de Storage (van en `storage-policies.sql`) y el historial de migraciones necesita también el esquema `supabase_migrations`. El paso de archivos queda a la espera del destino (12.1–12.2). Ese ensayo **no** cuenta para 12.5: fue sobre la base local, no sobre una copia de producción.
- [ ] 12.5 **Ejecutar** la restauración sobre un entorno limpio a partir de una copia real y comprobar que el sistema queda operativo. Cubre → *A restore has been performed…* → «The restored system is operational».
- [ ] 12.6 Comparar los conteos de `inventory_movements` y `activity_log` entre origen y restaurado. Cubre → «Restoring does not fire the triggers again».
- [ ] 12.7 Firmar el ensayo en la bitácora de `docs/recuperacion.md`: fecha, origen, entorno, pasos, incidencias y duración. Cubre → «The rehearsal is recorded, not just described».

## 13. Cierre

- [ ] 13.1 `npm run lint`, `npm run typecheck`, `test:unit`, `test:integration`, `supabase test db` y `test:e2e` en verde, y trabajo de estabilidad en verde.
- [x] 13.2 Regenerar el grafo con `graphify update .` tras las migraciones de los grupos 7 y 8.
  - *Hecho:* `graphify update .` después de las cinco migraciones y de los últimos cambios de código.
- [ ] 13.3 Repasar la definición de terminado del backlog punto por punto, incluida la comprobación de que ningún valor derivado quedó almacenado y de que no se introdujo ningún concepto ausente del modelo conceptual.
  - *Repaso del 2026-09-11 (se repite al cerrar):*
    - Cambio de OpenSpec existe y valida; alcance y decisiones cerradas con la persona usuaria (`/opsx:update`). ✓
    - Criterios del backlog con prueba: 1 recorridos estables → suite completa dos veces seguidas sin reintentos (624/624), falta verlo en CI · 2 estados → `view-states.spec.ts`, `app/route-states.test.tsx`, `components/shared/states.test.tsx` ✓ · 3 `security_invoker` → `views_security.test.sql` ✓ · 4 restauración → procedimiento ejecutado en local; **falta el ensayo con copia de producción (12.5–12.7)** · 5 exportación → `export.test.ts`, `export-manifest.test.ts` ✓ · 6 accesibilidad → `accessibility.spec.ts` ✓ · 7 panel < 2 s → `performance.spec.ts` mide la visita de vuelta (~350 ms); **la primera visita en frío mide ~2,4 s y queda como pregunta abierta de `design.md`** · 8 anexo → `docs/anexo-bd-verificacion.md` y `preproduction_checklist.test.sql` ✓.
    - lint, typecheck, unitarias (2 075), pgTAP (924) y e2e en verde en local. Integración: una prueba intermitente (`reports.test.ts`) porque `task-deliverables.test.ts` escribe pedidos en Geeko en paralelo; otra sesión está aislando esas pruebas. **CI aún no ha corrido.**
    - Esquema: cinco migraciones nuevas, cada una con su pgTAP (`preproduction_checklist`, `activity_exports_access`, `export_log`, `signup_hook`); grafo regenerado. ✓
    - Permisos: la política de `activity-exports`, `record_export()`, el hook de alta y la exportación tienen pruebas de otra organización, de ayudante y sin sesión. ✓
    - Ningún concepto nuevo del dominio: `exported` es una acción más de la bitácora; las miniaturas y el esquema `e2e` son técnicos, y `e2e` no existe en producción. ✓
    - Ningún valor derivado en una columna ni en un store: las migraciones no añaden columnas ni tablas y ningún store cambió. ✓ Lo único derivado que se guarda es la **miniatura**, un archivo en Storage y no un valor: se considera aceptable porque un adjunto no se edita —cambiar la foto es subir otra—, así que no puede quedar desactualizada.
    - Archivado: pendiente (13.4).
    - Queda sin marcar hasta que los pendientes de arriba se resuelvan.
- [ ] 13.4 Archivar el cambio de OpenSpec.
